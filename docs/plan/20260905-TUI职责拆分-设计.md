# TUI 职责拆分设计

## 本轮范围

用户确认按架构建议拆分。第一轮仅外置布局样式并组件化命令预览，保留消息渲染、历史、请求 Worker 和取消代次的现有实现。

## 职责与依赖

- `application.py` 负责应用组装、事件分发、命令执行和请求生命周期。
- `command_palette.py` 的 `CommandPalette` 继承 `Static`，维护候选、选择索引、已关闭输入和文本展示。接口为 `filter_input`、`move_selection`、`take_selection`、`dismiss` 及 `is_open`。
- 主应用在输入变化时传入原文和空闲状态，在方向键、Tab、Enter、Esc 时协调候选与原有交互。
- `styles/application.tcss` 与 `styles/approval.tcss` 分别由所属类通过 `CSS_PATH` 加载，路径相对于类所在模块，不依赖启动工作目录。
- Rich `Panel`、`Text` 样式保留在消息渲染代码；不机械转换为 TCSS。

## 验收任务

- [x] 原样迁移两份 CSS，样式外置后 75 项 TUI 测试通过。
- [x] 候选状态与渲染迁入组件，主应用不再保存候选列表、索引或关闭文本。
- [x] 更新架构知识与项目索引。
- 验证：沿用现有交互测试，不修改断言以适应重构；执行 compileall、完整 pytest、pip check 和差异检查。

## 后续边界

第一轮已提交为 `ccd255d`，第二轮消息区域拆分已提交为 `0bf53b1`。用户要求提交后继续拆分，第三轮推进输入历史状态抽取和活动栏组件化。

## 第二轮：消息区域组件化

- `Transcript` 继承现有可复制消息组件，接收角色和原文，负责用户卡片、Assistant Markdown、系统纯文本以及 Rich 样式。
- `StreamOutput` 维护文本片段和待刷新标记，提供追加、批量绘制、重置接口。
- 主应用保留请求代次校验、Worker、取消和 Timer 调度；只把已验证回调交给组件，保持旧结果无法写回的边界。
- 组件继承既有选择行为并保留原 ID、布局与配置；现有测试无需修改。
- 验证沿用完整 pytest、compileall、pip check 和差异检查。

## 第三轮：输入历史与活动栏

- `InputHistory` 是纯 Python 状态对象，保存输入记录、导航索引和草稿；`previous`/`next` 返回待展示文本，`None` 表示保持当前输入。
- `append`、`clear` 自动重置导航；磁盘清理仍须由会话先完成，失败时保留历史。
- `ActivityBar` 负责动画帧与思考/审批文案，使用主应用传入的耗时；不创建 Timer 或独立时钟。
- 主应用只协调方向键优先级、输入框更新、请求 Timer、代次和取消，避免出现多个生命周期管理者。
- 现有测试的用户行为断言不变，仅将历史内部状态断言迁移到所属对象。

## 第四轮：状态摘要与工作区变更追踪

- 第三轮已提交为 `9a22609`。
- `StatusBarState` 只包含 Provider、模型、配置可用性和运行状态等有限字段；`StatusBar` 独立负责单行格式化，不接触环境或敏感正文。
- `AppliedChangeTracker` 观察成功的编辑与撤销结果，安全解析 `change_id` 和相对路径，并返回去重排序后的当前路径集合。
- 主应用保留请求生命周期和错误消息展示，只把观察回调交给追踪器；模型失败时仍明确提示已经落盘的文件。
- 新增追踪器正常应用、撤销、去重以及非法结果忽略测试，并沿用 TUI 端到端失败提醒断言。

## 第五轮：输入提交职责整理

- `action_submit_prompt` 只保留交互优先级和流程分发，不再内联会话清理、阻断校验及 Worker 初始化。
- 审批按钮提交、本地命令消费、会话清理、请求前校验和请求启动分别使用具名方法表达意图。
- `/quit` 仍可在请求运行时取消并退出；`/clear` 与 `/skills` 仍只在空闲时执行，所有错误展示顺序保持不变。
- 现有命令、损坏历史、配置错误、重复发送和审批交互测试保持原断言并全部通过。

## 第六轮：本地命令目录独立

- `commands.py` 用枚举和不可变说明对象集中维护本地命令，提供与 Textual 无关的完整命令解析。
- `CommandPalette` 只负责基于统一目录过滤和展示候选，不再持有第二份命令名称与说明。
- 主应用按解析后的枚举分发副作用；清空会话、展示 Skill 和退出仍由拥有组件与生命周期的应用执行。
- 增加命令唯一性、说明非空、首尾空白兼容以及未知输入测试，原有 TUI 行为测试保持不变。
