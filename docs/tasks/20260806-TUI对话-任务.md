# 任务：TUI 对话入口

关联文档：

- [需求](../spec/20260806-TUI对话-需求.md)
- [设计](../plan/20260806-TUI对话-设计.md)

状态：已完成。

## Task 1：建立依赖与测试基线

- [x] 将 `python-dotenv==1.2.2` 和 `textual==8.2.8` 加入依赖文件，不升级现有依赖。
- [x] 在干净或可解释的 Python 3.11 环境安装依赖。
- [x] 运行现有 17 个测试，记录改造前 HTTP 契约基线。
- [x] 验证 Textual、python-dotenv 和现有应用可导入。

Acceptance：

- 新依赖版本与设计一致。
- 现有依赖版本没有被顺带修改。
- 现有 HTTP 测试在代码改造前全部通过。
- 安装或 `pip check` 的环境级冲突被明确区分，不被误报为项目通过。

Verify：

```bash
/Users/wangfei/anaconda3/bin/python3 -m pip install -r requirements.txt
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /Users/wangfei/anaconda3/bin/python3 -m pytest -q tests/test_chat.py
/Users/wangfei/anaconda3/bin/python3 -c 'import dotenv, textual; from main import app; assert app is not None'
```

Files：

- `requirements.txt`

## Task 2：测试驱动建立共享 Runtime 和中立异常

- [x] 先创建 Runtime 行为测试，确认因模块尚不存在而失败。
- [x] 将阿里云 Service 的 FastAPI 异常替换为 Provider 异常。
- [x] 新增 `ChatResult`、`ChatErrorCode`、`ChatRuntimeError` 和 `run_chat()`。
- [x] 覆盖成功、空白输入、配置、超时、连接、鉴权、其他状态和无效 JSON。
- [x] 确认 Runtime 不导入 FastAPI 或 Textual。

Acceptance：

- Runtime 是 FastAPI 与 TUI 可共同调用的单轮用例边界。
- Service 只描述 Provider 失败，不创建界面异常。
- 所有错误只携带安全消息与必要状态，不包含 Key 或上游错误体。
- Runtime 测试先红后绿，且不访问真实网络。

Verify：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /Users/wangfei/anaconda3/bin/python3 -m pytest -q tests/test_chat_runtime.py
rg -n 'fastapi|textual' app/runtime
```

Files：

- `app/services/aliyun_responses.py`
- `app/runtime/__init__.py`
- `app/runtime/chat.py`
- `tests/test_chat_runtime.py`

## Task 3：将 HTTP Router 接入 Runtime 并保持契约

- [x] 先调整测试注入边界，使现有 HTTP 行为继续由可控假上游验证。
- [x] Router 改为只调用 `run_chat()`。
- [x] 在 HTTP 边界将 Runtime 错误映射回原状态码与 `detail`。
- [x] 运行全部 HTTP 回归，修复任何契约差异。

Acceptance：

- `/chat` 请求、422、成功响应和全部错误映射与改造前一致。
- Router 不包含阿里云请求 Header、Payload 或 HTTPX 逻辑。
- Router 不直接调用 Service。
- 17 个既有测试全部通过。

Verify：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /Users/wangfei/anaconda3/bin/python3 -m pytest -q tests/test_chat.py
rg -n 'aliyun_responses|httpx|Authorization' app/routers
```

Files：

- `app/routers/chat.py`
- `tests/test_chat.py`

## Task 4：测试驱动实现 TUI 核心与启动入口

- [x] 先为响应正文提取、JSON fallback 和状态定义编写失败测试。
- [x] 新增 `Ready`、`Thinking`、`Error` 状态。
- [x] 实现标题、对话记录、中文输入区、状态栏和快捷键 Footer。
- [x] 实现 Enter 提交、Esc 退出和单 Worker 异步调用。
- [x] 在 Textual 导入前关闭 Kitty 扩展键盘协议，支持输入框直接接收 macOS 中文输入法文本，并增加启动顺序回归测试。
- [x] 实现项目根目录 `.env` 加载和 `python3 -m app.tui` 入口。

Acceptance：

- TUI 可独立启动，不依赖 Uvicorn。
- 网络等待不阻塞 Textual 事件循环。
- 输入和响应按安全纯文本写入记录区。
- 状态栏不显示 Key 内容。
- 文本提取失败时显示完整可读 JSON。

Verify：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /Users/wangfei/anaconda3/bin/python3 -m pytest -q tests/test_tui.py -k 'presentation or initial or submit'
/Users/wangfei/anaconda3/bin/python3 -m compileall -q app/tui
```

Files：

- `app/tui/__init__.py`
- `app/tui/__main__.py`
- `app/tui/application.py`
- `app/tui/state.py`
- `tests/test_tui.py`

## Task 5：完成 TUI 命令、错误恢复与取消测试

- [x] 为 `/clear`、`/quit`、未知斜杠文本、重复提交和 Esc 编写失败测试。
- [x] 只拦截精确的 `/clear` 与 `/quit`。
- [x] 确认 `/help`、`/chat` 和其他文本作为普通模型输入。
- [x] Thinking 时阻止第二次请求。
- [x] 实现已知错误、未知错误、失败后重试和取消恢复。
- [x] 验证取消完成竞态不会写入陈旧结果。

Acceptance：

- 不存在 `/help` 或 `/chat` 本地命令。
- 同一 TUI 同时最多一个模型请求。
- Esc 在忙碌时取消请求并退出，空闲时直接退出。
- 错误和取消不会关闭应用或泄露堆栈、Key。
- 全部 TUI 无头测试通过且不访问真实网络。

Verify：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /Users/wangfei/anaconda3/bin/python3 -m pytest -q tests/test_tui.py
```

Files：

- `app/tui/application.py`
- `tests/test_tui.py`

## Task 6：同步文档并完成全量验收

- [x] 更新 README 的安装、HTTP/TUI 启动、编辑、命令和快捷键。
- [x] 更新 Architecture 的双入口共享 Runtime 依赖方向。
- [x] 更新 Overview 的依赖与首期功能边界。
- [x] 更新 Troubleshooting 的 Python、终端、Key 和启动排障。
- [x] 将需求、设计、任务和索引状态更新为已完成。
- [x] 运行语法、全量测试、依赖、差异和敏感信息检查。
- [x] 按正确性、架构、可读性、安全、可靠性和性能完成代码审查。

验证结果：

- Python 语法检查通过。
- 全量测试 `44 passed`，无跳过、无真实模型调用。
- Textual 8.2.8、python-dotenv 1.2.2 安装版本与声明一致。
- 真实 TTY 冒烟成功：界面挂载，空闲 `Esc` 正常退出，未发送模型请求。
- `git diff --check`、尾随空白、依赖方向和敏感信息检查通过。
- `pip check` 仍报告共享 Anaconda 环境中已存在的多项全局冲突；没有发现由本次两个新增依赖直接造成的新冲突。

Acceptance：

- 文档只描述真实已实现能力，启动命令可以复制执行。
- 全部自动化测试通过，无跳过、无真实外部网络。
- Python 文件语法检查通过。
- 本次新增依赖无冲突；既有全局环境冲突单独说明。
- Git 差异没有空白错误、真实密钥或范围外变更。
- 实现满足批准需求和设计，没有增加 `/help`、`/chat`、流式、多轮或持久化。

Verify：

```bash
/Users/wangfei/anaconda3/bin/python3 -m compileall -q main.py app tests
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /Users/wangfei/anaconda3/bin/python3 -m pytest -q
/Users/wangfei/anaconda3/bin/python3 -m pip check
git diff --check
git status --short
```

Files：

- `README.md`
- `docs/knowledge/architecture.md`
- `docs/knowledge/project-overview.md`
- `docs/knowledge/troubleshooting.md`
- `docs/spec/README.md`
- `docs/plan/README.md`
- `docs/tasks/README.md`
- `docs/tasks/20260806-TUI对话-任务.md`

## Execution Gate

- 严格按 Task 1 → Task 6 顺序推进，每个任务验证通过后再进入下一项。
- 行为代码遵循 Red → Green → Refactor；先观察目标测试因缺失行为而失败。
- 不在本任务中创建 Git Commit、升级现有依赖或访问真实阿里云接口。
- 用户批准本任务清单后，方可进入 Phase 4（Implement）。
