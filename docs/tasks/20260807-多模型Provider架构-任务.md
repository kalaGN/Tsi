# 多模型 Provider 架构任务

状态：已完成（2026-08-07）

- 对应需求：[多模型 Provider 架构需求](../spec/20260807-多模型Provider架构-需求.md)
- 对应设计：[多模型 Provider 架构设计](../plan/20260807-多模型Provider架构-设计.md)

## 执行原则

- 按任务编号顺序实施；后一项依赖前一项稳定接口。
- 每项先增加或修改测试并确认目标行为失败，再完成最小实现。
- 所有模型请求使用 HTTPX MockTransport，禁止调用真实阿里云或 DeepSeek。
- 每项只修改列出的主题文件；发现设计不成立时先更新设计并重新确认。
- 代码增加必要注释，但不逐行复述实现。
- 实施期间不创建 Git Commit，除非用户另行明确要求。

## Task 0：建立变更前基线

- [x] 运行当前语法检查、全量测试、依赖检查和差异检查。
- [x] 记录基线测试数量；确认当前失败不是由本次实现造成。
- [x] 确认 `.env`、真实密钥和缓存文件未进入 Git 变更。

验收标准：当前代码基线可复现，且工作区仅包含本次 Spec、设计和任务文档。

验证：

```bash
.venv/bin/python -m compileall -q main.py app tests
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
.venv/bin/python -m pip check
git diff --check
git status --short
```

涉及文件：无代码文件。

## Task 1：建立共享 Provider 契约与配置解析

- [x] 在测试中先定义默认 DeepSeek、显式阿里云、大小写/空格归一化、空白/非法 Provider、模型默认值/覆盖值和 Key 状态的解析结果。
- [x] 创建 `LlmProvider`、`ProviderResult` 以及共享安全异常。
- [x] 实现环境映射到不可变 Provider 配置的解析；具体 Provider 实例创建随 Task 2、Task 3 接入。
- [x] 确认契约类型不读取环境、不依赖 FastAPI、Textual 或具体 Provider 协议。

验收标准：配置解析能确定性地得到 Provider、模型和 Key 状态；非法或空白 `LLM_PROVIDER` 安全失败；密钥不会出现在公开状态或异常中。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_providers.py -k factory
.venv/bin/python -m compileall -q app/services/llm tests/test_llm_providers.py
```

涉及文件：

- `app/services/llm/__init__.py`
- `app/services/llm/contracts.py`
- `app/services/llm/factory.py`
- `tests/test_llm_providers.py`

## Task 2：实现共享 HTTP 边界与阿里云适配器

- [x] 先为阿里云 URL、Header、默认/覆盖模型、请求 JSON 和三类响应文本形态增加失败测试。
- [x] 为缺少密钥、超时、连接失败、401/403、其他非成功状态、非 JSON 和无文本响应增加失败测试。
- [x] 实现共享异步 JSON POST、固定超时和脱敏错误映射。
- [x] 将阿里云请求与文本提取迁入 Provider 类。

验收标准：阿里云 Provider 返回稳定 `output_text` 与内部原始响应；缺少 Key 时没有网络调用；现有安全错误语义保持。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_providers.py -k 'http or aliyun'
.venv/bin/python -m compileall -q app/services/llm tests/test_llm_providers.py
```

涉及文件：

- `app/services/llm/contracts.py`
- `app/services/llm/http_client.py`
- `app/services/llm/aliyun.py`
- `app/services/llm/factory.py`
- `tests/test_llm_providers.py`

## Task 3：实现 DeepSeek 适配器

- [x] 先为官方 URL、Bearer Header、默认/覆盖模型、单轮 `messages` 和 `stream: false` 增加失败测试。
- [x] 为标准文本提取及 `choices/message/content` 异常结构增加失败测试。
- [x] 覆盖 DeepSeek 401、402、422、429、500、503、超时、连接失败和非 JSON 响应。
- [x] 实现 DeepSeek Chat Completions Provider，并接入工厂。

验收标准：DeepSeek Provider 使用指定协议返回稳定文本；所有错误脱敏且保留应保留的上游状态码；无真实网络调用。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_providers.py -k deepseek
.venv/bin/python -m compileall -q app/services/llm tests/test_llm_providers.py
```

涉及文件：

- `app/services/llm/deepseek.py`
- `app/services/llm/factory.py`
- `app/services/llm/__init__.py`
- `tests/test_llm_providers.py`

## Task 4：迁移 Chat Runtime

- [x] 先把 Runtime 测试改为注入 Fake Provider，覆盖成功、空白输入和全部共享异常。
- [x] 将 `ChatResult` 改为 `output_text`、Provider 和模型。
- [x] 增加 `ChatRuntimeInfo` 与安全配置读取接口。
- [x] 移除 Runtime 对阿里云模块、常量和专属异常的依赖。

验收标准：Runtime 只依赖统一 Provider 契约和工厂；直接调用方得到稳定文本；配置和 Provider 错误映射正确。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat_runtime.py
.venv/bin/python -m compileall -q app/runtime app/services/llm tests/test_chat_runtime.py
```

涉及文件：

- `app/runtime/chat.py`
- `app/services/llm/contracts.py`
- `app/services/llm/factory.py`
- `tests/test_chat_runtime.py`

## Task 5：迁移 `/chat` 为统一文本响应

- [x] 先将 HTTP 成功测试改为期待唯一字段 `output_text`，并覆盖阿里云与 DeepSeek。
- [x] 增加断言，确保原始 `id`、`output`、`choices`、usage 和 reasoning 不进入响应。
- [x] 增加显式 `ChatResponse`，Router 仅包装 Runtime 的 `output_text`。
- [x] 回归请求校验和全部 HTTP 错误映射。

验收标准：两个 Provider 的 `/chat` 成功响应都固定为 `200` 与 `{"output_text": "..."}`；请求结构和错误结构保持既定规则。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat.py
.venv/bin/python -m compileall -q app/routers app/runtime tests/test_chat.py
```

涉及文件：

- `app/routers/chat.py`
- `app/runtime/chat.py`
- `tests/test_chat.py`

## Task 6：迁移 TUI 到统一 Runtime 结果

- [x] 先把 TUI 测试改为注入 `ChatRuntimeInfo`，覆盖两种 Provider、模型、Key 状态和配置错误。
- [x] 删除 JSON 响应格式化测试，改为断言直接显示 `ChatResult.output_text`。
- [x] 移除 TUI 对 Provider 环境变量、固定模型和上游响应结构的依赖。
- [x] 回归中文输入、Enter、双击 Esc、取消、`/clear`、`/quit` 和耗时统计。

验收标准：TUI 不包含阿里云或 DeepSeek 的响应解析；状态与实际 Provider 使用同一套配置规则；既有交互不回退。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py
.venv/bin/python -m compileall -q app/tui app/runtime tests/test_tui.py
```

涉及文件：

- `app/tui/application.py`
- `app/runtime/chat.py`
- `tests/test_tui.py`

## Task 7：清理旧实现并同步使用文档

- [x] 确认运行代码和测试不再引用旧 `aliyun_responses` 后删除该文件。
- [x] README 增加 Provider 选择、两套环境变量、DeepSeek 启动示例及统一响应迁移说明。
- [x] 更新 API 约定和排障文档，删除“固定阿里云/原样响应”等过期说明。
- [x] 搜索旧模块、硬编码 Provider 和旧响应契约的残留引用。

验收标准：用户能仅依据 README 配置任一 Provider；公开 API 与排障文档和代码一致；历史 Spec/Plan 保留历史事实，不改写。

验证：

```bash
rg -n "aliyun_responses|固定使用阿里云|原样返回上游|CHAT_PROVIDER|CHAT_MODEL" README.md app tests docs/knowledge docs/rules
git diff --check
```

涉及文件：

- `app/services/aliyun_responses.py`（删除）
- `README.md`
- `docs/knowledge/api-conventions.md`
- `docs/knowledge/troubleshooting.md`

## Task 8：同步架构知识与项目规则

- [x] 更新架构图、依赖方向、组件职责和模型调用流程。
- [x] 更新项目概览与领域术语中的 Provider、模型和配置事实。
- [x] 更新 Rules 中的架构、HTTP 契约、测试和安全规则。
- [x] 只记录已实现能力，不把非目标描述成已经支持。

验收标准：AGENTS 索引指向的长期知识与最终代码一致，规则不再把 Runtime 或 TUI 绑定到阿里云。

验证：

```bash
rg -n "aliyun_responses|固定.*qwen3-max|原样返回上游|DASHSCOPE_API_KEY" AGENTS.md docs/knowledge docs/rules
git diff --check
```

涉及文件：

- `AGENTS.md`
- `docs/knowledge/architecture.md`
- `docs/knowledge/project-overview.md`
- `docs/knowledge/domain-glossary.md`
- `docs/rules/harness-engineering-rules.md`

## Task 9：全量验证与任务归档

- [x] 运行语法检查、全量测试、依赖检查和差异检查。
- [x] 检查 `.env`、真实密钥、Authorization 值和缓存文件未进入变更。
- [x] 更新本任务复选框、最终测试数量和验证结果。
- [x] 将需求、设计、任务及索引状态更新为已完成。

验收标准：全部质量门禁通过，无真实外部请求、无敏感信息、无旧运行模块引用；Git 变更只包含本需求范围。

验证：

```bash
.venv/bin/python -m compileall -q main.py app tests
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
.venv/bin/python -m pip check
git diff --check
git status --short
```

涉及文件：

- `docs/spec/README.md`
- `docs/plan/README.md`
- `docs/tasks/README.md`
- `docs/tasks/20260807-多模型Provider架构-任务.md`

## 完成定义

- [x] DeepSeek 是无配置时的默认 Provider，阿里云可显式选择。
- [x] DeepSeek 官方 Chat Completions 接入通过 Mock 测试。
- [x] `/chat` 与 TUI 都使用统一提取文本。
- [x] `/chat` 成功响应只有 `output_text`，不泄漏上游结构。
- [x] Provider 选择、模型和密钥规则与文档一致。
- [x] 两种 Provider 的正常、异常和边界测试完整。
- [x] TUI 中文输入、交互、取消和耗时功能没有回退。
- [x] 未新增依赖、未访问真实模型、未提交密钥。
- [x] 全量质量门禁通过，Knowledge 和 Rules 已同步。

## 最终验证结果

- Python：3.11.5（仓库本地 `.venv`）。
- 语法检查：通过。
- Pytest：`91 passed`。
- 依赖检查：`No broken requirements found.`。
- 差异检查：通过。
- 旧 `aliyun_responses`、`CHAT_PROVIDER`、`CHAT_MODEL`、TUI JSON 解析引用：运行代码与当前知识文档中均为零。
- `.env`、`.venv`、Pytest 与 Python 缓存：继续由 Git 忽略。
- 代码质量审查：正确性、可读性、架构、安全和性能五个维度通过；审查发现的 DeepSeek 空文本一致性问题已增加回归测试并修复。
