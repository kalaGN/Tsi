# 当前对话上下文任务

状态：已完成（2026-08-08）

- [x] 定义 Provider 中立消息契约和非法请求校验。
- [x] 让阿里云与 DeepSeek Adapter 支持有序多轮消息。
- [x] 增加版本化 JSON Session Store、原子写入和损坏数据处理。
- [x] 增加 `ChatSession` 的加载、发送、回滚、并发和清理语义。
- [x] 接入 TUI 自动恢复和 `/clear`，保持 HTTP 无状态。
- [x] 增加 Provider、Store、Session、HTTP 和 TUI 回归测试。
- [x] 更新 `.gitignore`、README、架构知识和文档索引。
- [x] 运行语法、全量测试、依赖和差异门禁。
