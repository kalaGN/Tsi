# 模型输入输出日志任务

状态：已完成（2026-08-07）

对应需求：[`docs/spec/20260807-模型调用日志-需求.md`](../spec/20260807-模型调用日志-需求.md)

对应设计：[`docs/plan/20260807-模型调用日志-设计.md`](../plan/20260807-模型调用日志-设计.md)

## 任务

- [x] T1：扩展日志事件契约
  - 验收：`llm_request` 增加完整 `input_text`，新增 `llm_response` 及完整 `output_text`，两类事件使用精确白名单。
  - 验收：中文、引号和换行可无损还原，日志仍是单行 JSON；双写、幂等和转储保持不变。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_model_logging.py`
  - 文件：`app/observability/model_logging.py`、`tests/test_model_logging.py`

- [x] T2：在 Runtime 关联输入和输出
  - 依赖：T1。
  - 验收：成功流程按 request log → Provider → response log 执行，两条日志 request ID 一致。
  - 验收：Provider 失败只写 request；空输入和创建 Provider 失败不写日志。
  - 验收：不记录 API Key、原始响应、异常原文或 Traceback。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat_runtime.py tests/test_model_logging.py`
  - 文件：`app/runtime/chat.py`、`tests/test_chat_runtime.py`

- [x] T3：同步文档并执行全量回归
  - 依赖：T1、T2。
  - 验收：README 明确警知日志包含明文内容和文件权限风险；架构知识与真实实现一致。
  - 验收：HTTP JSON/Header、Provider 契约和 TUI 交互不变。
  - 验证：

    ```bash
    .venv/bin/python -m compileall -q main.py app tests
    PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
    .venv/bin/python -m pip check
    git diff --check
    ```

  - 文件：`README.md`、`docs/knowledge/architecture.md`、Spec/设计/任务索引

## 实施边界

- 不修改 `/chat` JSON 或 Header 契约。
- 不修改 Provider 签名或上游 Header。
- 不记录 API Key、Authorization、Provider 原始对象、异常或 Traceback。
- 不新增内容脱敏、截断、开关或第三方依赖。
