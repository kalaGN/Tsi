# 外部模型请求日志任务

状态：已完成（2026-08-08）

对应[需求](../spec/20260808-外部模型请求日志-需求.md)和[设计](../plan/20260808-外部模型请求日志-设计.md)。

## Task 1：扩展结构化日志契约

- [x] 在事件字段白名单中增加 `llm_http_request`、`llm_http_response` 和 `llm_http_error`。
- [x] 增加三个类型化记录函数，请求函数只构造脱敏 Header，不接收真实 Authorization。
- [x] 测试精确字段、完整 `messages`、中文/换行往返、request ID 和假密钥不泄漏。
- 验收：新事件为单行 JSON，stderr 与文件一致，现有事件不变。
- 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_model_logging.py`
- 文件：`app/observability/model_logging.py`、`tests/test_model_logging.py`

## Task 2：在共享 HTTP 边界记录真实调用

- [x] 扩展 `post_json()` 的 request ID、Provider、模型和可测时钟参数。
- [x] 在 `client.post()` 前记录完整 payload、URL、POST、脱敏 Header 和 10/60 秒超时。
- [x] 收到任意 HTTP 响应后记录状态、Content-Type 和耗时；网络异常只记录安全分类和耗时。
- 验收：非 2xx 不重复记错误事件，原始响应体/异常/API Key 不进 Logger。
- 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_providers.py tests/test_model_logging.py`
- 文件：`app/services/llm/http_client.py`、`tests/test_llm_providers.py`、`tests/test_model_logging.py`

## Task 3：贯通 Runtime、Provider 和 HTTP 的 request ID

- [x] Provider 契约增加必填关键字 `request_id`。
- [x] Runtime 将已有 ID 传给 Provider，阿里云和 DeepSeek 再把 ID/provider/model 传给 `post_json()`。
- [x] 更新 Fake Provider，验证四个成功事件的 ID 一致和固定顺序。
- 验收：DeepSeek `messages` 和阿里云 `input` 日志与 MockTransport 收到的 JSON 完全一致。
- 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat_runtime.py tests/test_llm_providers.py`
- 文件：`app/runtime/chat.py`、`app/services/llm/contracts.py`、`app/services/llm/aliyun.py`、`app/services/llm/deepseek.py`、`tests/test_chat_runtime.py`

## Task 4：执行 HTTP、Session 和 TUI 回归

- [x] 更新其余 Fake Provider 签名，不弱化现有断言。
- [x] 验证 HTTP `/chat` 的 JSON/状态/Header 契约不变。
- [x] 验证 TUI 多轮恢复、`/clear`、取消和 Session 原子提交不变。
- 验收：所有测试仍只使用 Fake/MockTransport，不访问真实模型。
- 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat.py tests/test_chat_session.py tests/test_tui.py`
- 文件：`tests/test_chat.py`、`tests/test_chat_session.py`、`tests/test_tui.py`

## Task 5：同步文档并运行全量门禁

- [x] README 说明新事件顺序、完整 `messages/input` 明文、脱敏 Header、响应和错误边界。
- [x] 更新架构知识和 Spec/设计/任务状态及索引。
- [x] 执行全量语法、测试、依赖和差异检查。
- 验收：文档与实现一致，无真实密钥或会话运行数据进入 Git。
- 验证：

  ```bash
  .venv/bin/python -m compileall -q main.py app tests
  PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
  .venv/bin/python -m pip check
  git diff --check
  ```

- 文件：`README.md`、`docs/knowledge/architecture.md`、`docs/spec/`、`docs/plan/`、`docs/tasks/`

## 实施顺序

`Task 1 → Task 2 → Task 3 → Task 4 → Task 5`。其中 Task 1 必须先确定日志契约；Task 2/3 依赖该契约；Task 4 在签名迁移后执行；Task 5 收口。

