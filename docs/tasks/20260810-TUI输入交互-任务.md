# TUI 输入交互任务

状态：已完成（2026-08-10）

对应[需求](../spec/20260810-TUI输入交互-需求.md)和[设计](../plan/20260810-TUI输入交互-设计.md)。

## Task 1：锁定活动栏生命周期

- [x] 增加输入框上方固定活动栏的布局测试。
- [x] 增加请求开始、实时时钟推进和成功清理测试。
- [x] 增加已知错误、未知错误与 Esc 取消清理测试。

验收：测试能在实现前暴露活动栏缺失，实现后证明 Timer 在所有路径停止且最终耗时不回归。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py -k activity
```

可能修改：`tests/test_tui.py`。

依赖：无。规模：S。

## Task 2：实现请求专属活动 Timer

- [x] 在 transcript 和 prompt 之间加入 `#activity-bar`。
- [x] 启动请求时创建 Timer 并实时渲染动画、思考中、耗时和 Esc 提示。
- [x] 在成功、异常、取消和退出路径按请求代次停止并清空。

验收：空闲没有运行 Timer，活动提示约每 100 ms 更新，旧 Worker 不能影响新请求。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py -k 'activity or escape or error'
```

可能修改：`app/tui/application.py`、`tests/test_tui.py`。

依赖：Task 1。规模：S。

## Checkpoint 1：活动状态

- [x] 请求期间提示和实时时钟可见。
- [x] 所有终止路径清理，现有取消和最终耗时测试通过。

## Task 3：锁定上下键历史语义

- [x] 增加空历史、顺序、双向边界和草稿恢复测试。
- [x] 增加 Session 恢复、失败/取消、`/clear` 成败测试。
- [x] 增加中文、多行、重复输入和光标末尾测试。

验收：测试覆盖需求中的每个历史来源和状态转换，不依赖真实模型。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py -k 'history or previous_input or next_input'
```

可能修改：`tests/test_tui.py`。

依赖：Task 2。规模：S。

## Task 4：实现输入历史导航

- [x] 从 Session user 消息初始化历史，并在普通请求启动时追加原文。
- [x] 实现高优先级 Up/Down、不循环边界、草稿恢复和光标末尾定位。
- [x] `/clear` 成功时清空历史，失败时保持。

验收：当前进程和跨重启来源符合 Spec，不改 Session schema 或失败回滚语义。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py
```

可能修改：`app/tui/application.py`、`tests/test_tui.py`。

依赖：Task 3。规模：S。

## Checkpoint 2：完整 TUI 交互

- [x] 活动栏、历史导航、中文、Markdown、Enter、双 Esc、`/clear`、`/quit` 全部通过。
- [x] 新增方法均有简洁中文 docstring。

## Task 5：同步文档与完成验证

- [x] README 更新活动栏、Up/Down 和已知边界。
- [x] Architecture、Project Overview 和 Troubleshooting 同步真实行为。
- [x] 更新 Spec/Plan/Tasks 状态与索引。
- [x] 执行全量门禁和五轴代码审查。

验收：文档与代码一致，无阻塞审查发现，未修改 HTTP/Provider/工具/Session schema/依赖。

验证：

```bash
.venv/bin/python -m compileall -q main.py app tools tests
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
.venv/bin/python -m pip check
git diff --check
```

可能修改：`README.md`、`docs/knowledge/*.md`、SDD 文档及索引。

依赖：Task 4。规模：M。
