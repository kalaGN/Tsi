# 模型流式输出任务

状态：已完成（2026-08-11）  
创建日期：2026-08-11

对应[需求](../spec/20260811-模型流式输出-需求.md)和[设计](../plan/20260811-模型流式输出-设计.md)。

## 依赖图

```text
Task 1 SSE/契约
      |
      +--> Task 2 DeepSeek
      |          |
      +--> Task 3 Aliyun
                 |
                 v
          Task 4 Runtime/工具
                 |
                 v
          Task 5 Session
                 |
                 v
          Task 6 TUI
                 |
                 v
          Task 7 HTTP/文档/审查
```

共享契约和同一测试文件存在顺序依赖，本次不并行实施。

## Task 1：有界 SSE 读取与中立回调契约

描述：先以失败测试锁定 UTF-8、SSE 帧、状态映射、整体超时、大小上限、日志和取消，
再实现 Provider 可复用的 `post_sse()` 与中立文本回调类型。

- [x] 增加中文跨 Byte Chunk、LF/CRLF、多行 data、注释、`[DONE]` 和空事件测试。
- [x] 增加非法 UTF-8、未闭合事件、64 KiB 边界、HTTP 状态、连接、60 秒整体超时与取消测试。
- [x] 实现 `post_sse()`、固定边界和 `TextDeltaHandler` / `TextResetHandler`，保持日志脱敏且每次流只记录一次完成事件。

验收：共享层只交付完整 `data` 字符串，严格关闭流；取消原样传播，解析错误不伪装为网络错误。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_providers.py -k 'sse or http'
```

可能修改：`app/services/llm/contracts.py`、`app/services/llm/http_client.py`、`tests/test_llm_providers.py`。

依赖：无。规模：M。

## Task 2：DeepSeek 流式文本与工具调用

描述：把 DeepSeek 请求切到 `stream: true`，解析 Chat Completions SSE，实时传递最终文本
Delta，并按 index 组装跨 Chunk 工具调用。

- [x] 先增加文本、usage 空 choice、reasoning 忽略、`[DONE]`、缺失终止和空回答测试。
- [x] 增加单个/多个工具调用的 ID、名称、参数跨 Chunk 拼接及续接消息测试。
- [x] 实现 DeepSeek 单步累加器、1 MiB 文本与 8 KiB 参数边界，完成后返回既有 `ModelStep`。

验收：回调 Delta 顺序与最终 `output_text` 完全一致；工具调用保持现有续接语义，reasoning 不外泄。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_providers.py -k deepseek
```

可能修改：`app/services/llm/deepseek.py`、`tests/test_llm_providers.py`。

依赖：Task 1。规模：S。

## Task 3：阿里云 Responses 流式文本与工具调用

描述：把阿里云请求切到 `stream: true`，解析 `output_text.delta/done`、function call 与
`response.completed`，并以完成对象校验增量结果。

- [x] 先增加文本 Delta、done/completed、一致性、无关事件和缺失完成事件测试。
- [x] 增加完整/增量 function call、多个调用、非法完成状态和空回答测试。
- [x] 实现阿里云流式状态累加、完成对象校验和既有 input 续接结构。

验收：文本可实时回调且完整结果稳定；有无参数 Delta 都能从完成事件得到同一合法工具调用。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_providers.py -k aliyun
```

可能修改：`app/services/llm/aliyun.py`、`tests/test_llm_providers.py`。

依赖：Task 1。规模：S。

## Checkpoint 1：协议与 Provider

- [x] 两家请求 Body 都是 `stream: true`，测试不调用真实接口。
- [x] SSE 边界、文本、工具、错误、超时、取消和日志定向测试通过。
- [x] `app/services/llm/` 不依赖 Runtime、TUI 或 Router。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_providers.py tests/test_model_logging.py
```

## Task 4：Runtime 与工具步骤流式编排

描述：把中立回调穿过 Runtime 和 Tool Loop；最终步骤持续输出，工具步骤在执行前撤销，
成功仍只记录一次完整 `llm_response`。

- [x] 先增加无工具 Delta 顺序、工具步骤 reset、工具上限、失败和取消日志测试。
- [x] 扩展 `run_tool_loop()`、`run_chat()`、`run_chat_messages()` 的可选回调并保持默认聚合行为。
- [x] 验证工具步骤中间文本不进入最终 `ChatResult`，任何失败不生成成功日志。

验收：HTTP 不注册回调时行为不变；注册回调时只暴露中立文本和 reset，不暴露 Provider 事件。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tool_loop.py tests/test_chat_runtime.py
```

可能修改：`app/runtime/tool_loop.py`、`app/runtime/chat.py`、`tests/test_tool_loop.py`、`tests/test_chat_runtime.py`。

依赖：Task 2、Task 3。规模：M。

## Task 5：Session 完整轮次提交

描述：让 Session 透传文本观察器，同时保持发送锁、取消检查、文件保存和内存提交顺序。

- [x] 先增加成功 Delta、工具 reset、流失败、取消和存储失败时的历史一致性测试。
- [x] 扩展 `ChatSession.send()` 的可选回调并保持 Session JSON schema 不变。
- [x] 验证只有完整成功回答写入磁盘和内存，部分输出永不落盘。

验收：跨重启恢复只出现完整轮次；失败、取消和存储错误后历史与调用前一致。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat_session.py
```

可能修改：`app/runtime/session.py`、`tests/test_chat_session.py`。

依赖：Task 4。规模：S。

## Checkpoint 2：Runtime 与持久化

- [x] Runtime、工具循环、日志和 Session 定向测试通过。
- [x] `ChatResult`、`ChatMessage` 和 Session JSON schema 没有变化。
- [x] 取消与错误不会留下完整日志或半轮会话。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tool_loop.py tests/test_chat_runtime.py tests/test_chat_session.py
```

## Task 6：TUI 临时流式输出与清理

描述：增加最高 8 行的临时纯文本 `RichLog`，通过现有 100ms Timer 合并刷新，并在成功、
工具 reset、错误、取消和退出路径正确清理。

- [x] 先增加初始隐藏、首 Delta、顺序追加、批量刷新、最终 Markdown 替换测试。
- [x] 增加工具 reset、错误、Esc 取消、双 Esc 退出和陈旧请求代次测试。
- [x] 实现临时组件、流缓存、Runner 新签名和全生命周期清理，保持中文、多行与半截 Markdown 原样。

验收：生成期间可观察到连续文本；完成后 transcript 只有一份最终 Assistant Markdown，临时区域为空且隐藏。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py
```

可能修改：`app/tui/application.py`、`tests/test_tui.py`。

依赖：Task 5。规模：S。

## Task 7：HTTP 回归、知识同步与交付审查

描述：锁定 `/chat` 聚合 JSON 契约，更新启动说明与架构事实，并完成五轴代码审查和全量门禁。

- [x] 更新 HTTP Mock 流测试，断言请求 `stream: true`、成功响应仍仅含 `output_text`、错误映射不变。
- [x] 更新 README、架构、项目概览、故障排查及 Spec/Plan/Task 状态，禁止描述未实现的 HTTP SSE。
- [x] 执行完整门禁、敏感信息检查、死代码检查和代码审查，处理所有阻断项。

验收：HTTP/TUI 使用同一流式上游；公开契约、依赖方向、安全日志和文档事实一致。

验证：

```bash
.venv/bin/python -m compileall -q main.py app tools tests
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
.venv/bin/python -m pip check
git diff --check
```

可能修改：`tests/test_chat.py`、`README.md`、`docs/knowledge/architecture.md`、`docs/knowledge/project-overview.md`、`docs/knowledge/troubleshooting.md`。

依赖：Task 6。规模：M。

## Checkpoint 3：完整交付

- [x] DeepSeek 与阿里云流式文本和工具调用全部通过 Mock 测试。
- [x] TUI 实时显示、最终 Markdown、取消、错误和恢复行为符合 Spec。
- [x] HTTP `/chat` 契约不变，日志和 Session 只保存完整结果。
- [x] 语法、全量测试、依赖和差异检查全部通过。
- [x] 任务清单全部勾选并标记完成，等待用户明确要求后再提交 Git。
