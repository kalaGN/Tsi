# 系统提示词任务

状态：已完成（2026-08-11）
创建日期：2026-08-11

对应[需求](../spec/20260811-系统提示词-需求.md)和[设计](../plan/20260811-系统提示词-设计.md)。

## Dependency Graph

```text
Task 1 文件读取
      |
      v
Task 2 消息契约与 Runtime
      |
      v
Task 3 Session 隔离
      |
      v
Task 4 TUI 装配
      |
      v
Task 5 文档、回归与审查
```

共享消息契约和 Session 存在顺序依赖，本次按依赖顺序实施。

## Task 1：有界系统提示词读取

- [x] 先覆盖缺失、空白、中文、32 KiB 边界、超限、非法 UTF-8、目录和读取失败测试。
- [x] 实现只读取启动目录直属 `AGENTS.md` 的标准库 loader 和安全错误。

验收：合法内容保持原文；非法内容在网络调用前失败，不泄露正文或绝对路径。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_system_prompt.py
```

可能修改：`app/runtime/system_prompt.py`、`tests/test_system_prompt.py`。依赖：无。规模：S。

## Task 2：system 消息契约与 Runtime 注入

- [x] 增加 system 首位正常序列和重复、错位、空白、无 user 等非法序列测试。
- [x] 扩展 `ChatRole` 与统一验证，并让 `run_chat_messages` 可选注入一次 system 消息。
- [x] 验证两家 Provider 请求体和完整请求日志包含相同 system 首位消息。

验收：两家 Provider 共享同一中立消息序列；HTTP `run_chat()` 不注入 system。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat_runtime.py tests/test_llm_providers.py
```

可能修改：`app/services/llm/contracts.py`、`app/runtime/chat.py`、相关测试。依赖：Task 1。规模：M。

## Task 3：Session 持久化隔离

- [x] 覆盖首轮、多轮、恢复和 clear 后 system 每次只发送一次。
- [x] Session 接收提示词但只提交和暴露 user/assistant；Store 继续拒绝 system。

验收：磁盘 schema 不变，系统提示词不会进入 `messages` 或 JSON。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat_session.py tests/test_session_store.py
```

可能修改：`app/runtime/session.py`、相关测试。依赖：Task 2。规模：S。

## Task 4：TUI 启动装配与阻断状态

- [x] 覆盖 cwd 读取、启动时单次装配、loaded/none/error 状态和错误时不启动请求。
- [x] TUI 入口装配 loader；Application 传递到 Session 并只展示安全状态。

验收：有效文件自动用于请求；非法文件仍可 `/quit`、`/clear`，但普通输入不调用 Runner。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py
```

可能修改：`app/tui/__main__.py`、`app/tui/application.py`、`tests/test_tui.py`。依赖：Task 3。规模：M。

## Task 5：文档、回归与完成审查

- [x] 更新 README、AGENTS、架构/概览/排障 Knowledge 和三类索引。
- [x] 将 Spec、设计和任务状态更新为已完成并运行全部真实门禁。
- [x] 按正确性、可读性、架构、安全和性能完成代码审查及需求逐项审计。

验收：所有 Success Criteria 都有源码或测试证据，工作区仅包含本需求改动。

验证：

```bash
.venv/bin/python -m compileall -q main.py app tools tests
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
.venv/bin/python -m pip check
git diff --check
```

可能修改：`README.md`、`AGENTS.md`、`docs/knowledge/`、索引和本需求文档。依赖：Task 4。规模：M。
