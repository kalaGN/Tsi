# TUI 双击复制单行任务

状态：已完成（2026-08-12）
创建日期：2026-08-12

对应已确认的[需求](../spec/20260812-TUI双击复制单行-需求.md)和
[设计](../plan/20260812-TUI双击复制单行-设计.md)。

## Task 1：组件行为

- [x] `SelectableRichLog` 增加默认关闭的双击复制选项。
- [x] 左键双击使用内容坐标和滚动偏移定位可见渲染行。
- [x] 有效行复制并只选择当前行；无效位置不覆盖剪贴板且不残留全选。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py -k 'double_click'
```

## Task 2：应用装配与隔离

- [x] 仅 `#transcript` 开启双击复制。
- [x] `#stream-output` 与审批 Diff 保持默认关闭。
- [x] 现有鼠标拖选和 `Ctrl/Cmd+C` 测试通过。

验证：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py
```

## Task 3：完成验收

- [x] 需求、设计、任务和索引状态同步为完成。
- [x] 全量测试、语法、依赖和差异检查通过。
- [x] 不自动创建 Git Commit。
