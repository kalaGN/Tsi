# 模型日志可读格式任务

## 状态

已完成（2026-08-12）。

## Task 1：建立文件格式回归测试

**依赖：** 无。

**验收标准：**

- [x] 文件事件为中文分块格式和北京时间。
- [x] JSON 内容缩进展示，普通文本保持换行。
- [x] stderr 单行 JSON 契约不变。

**验证：**

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_model_logging.py
```

**文件：** `tests/test_model_logging.py`

## Task 2：实现文件专属 Formatter

**依赖：** Task 1。

**验收标准：**

- [x] RotatingFileHandler 使用人类可读 Formatter。
- [x] StreamHandler 继续使用 JSON Formatter。
- [x] 八类事件均有稳定中文映射且不扩大字段白名单。

**验证：** Task 1 测试通过。

**文件：** `app/observability/model_logging.py`

## Task 3：验证入口、轮转和失败隔离

**依赖：** Task 2。

**验收标准：**

- [x] TUI 继续只保留文件 Handler。
- [x] HTTP 默认配置继续同时创建 Stream/File Handler。
- [x] 轮转及不可用文件测试通过。

**验证：**

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_model_logging.py tests/test_tui.py
```

**文件：** `tests/test_model_logging.py`、`tests/test_tui.py`

## Task 4：同步文档并完成门禁

**依赖：** Task 3。

**验收标准：**

- [x] README、Rules、Architecture 和 Troubleshooting 与真实格式一致。
- [x] 生成的样例日志可直接阅读且不包含测试假密钥之外的敏感值。
- [x] 全量质量门禁通过。

**验证：**

```bash
.venv/bin/python -m compileall -q main.py app tools tests
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
.venv/bin/python -m pip check
git diff --check
```

**文件：** `README.md`、`docs/rules/harness-engineering-rules.md`、`docs/knowledge/architecture.md`、`docs/knowledge/troubleshooting.md`
