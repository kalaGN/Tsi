# Tsi 助手项目命名任务

## 状态

已完成（2026-09-01）。

## Task 1：建立 FastAPI 名称契约

**说明：** 先用测试固定应用对象与 OpenAPI 文档的正式名称，再在既有应用组装边界设置名称。

**依赖：** 无。

**验收标准：**

- [x] `application_app.title` 等于 `Tsi 助手`。
- [x] `/openapi.json` 的 `info.title` 等于 `Tsi 助手`。
- [x] `GET /` 和 `POST /chat` 的既有契约不变。

**验证：**

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat.py
```

**可能修改文件：**

- `app/application.py`
- `tests/test_chat.py`

**规模：** S。

## Task 2：建立 TUI 名称契约

**说明：** 用无头 Textual 测试固定应用标题和顶部标题组件，再替换现有旧标题。

**依赖：** 无；实现阶段可在 Task 1 后独立执行。

**验收标准：**

- [x] `ChatTuiApp.TITLE` 等于 `Tsi 助手`。
- [x] 挂载后的 `#title` 可见内容等于 `Tsi 助手`。
- [x] 状态栏、输入和对话行为不变。

**验证：**

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py -k title
```

**可能修改文件：**

- `app/tui/application.py`
- `tests/test_tui.py`

**规模：** S。

## Checkpoint 1：运行时展示

- [x] Task 1 和 Task 2 的针对性测试通过。
- [x] 代码差异只包含 FastAPI 元数据、TUI 标题和对应测试。
- [x] 没有新增依赖或配置项。

## Task 3：同步当前项目文档

**说明：** 将正式名称写入当前入口、规则和知识文档，同时保留真实目录、模块、命令及历史文档。

**依赖：** Task 1、Task 2。

**验收标准：**

- [x] README 一级标题和介绍使用 `Tsi 助手`。
- [x] AGENTS、Rules 索引/正文和当前 Knowledge 明确正式名称。
- [x] `demo/` 路径、启动命令与历史 Spec/Plan/Tasks 未被重命名。

**验证：**

```bash
rg -n "FastAPI Demo|FastAPI Agent TUI|Tsi 助手" README.md AGENTS.md docs/rules docs/knowledge app tests
git diff --check
```

**可能修改文件：**

- `README.md`
- `AGENTS.md`
- `docs/rules/README.md`
- `docs/rules/harness-engineering-rules.md`
- `docs/knowledge/project-overview.md`
- `docs/knowledge/architecture.md`

**规模：** M，按当前文档白名单逐项修改，不做全仓替换。

## Task 4：完成全量验证与状态归档

**说明：** 验证项目行为未回归，审计最终差异，并把本次 Spec、Plan、Tasks 和索引标记为完成。

**依赖：** Task 3。

**验收标准：**

- [x] 全量测试、语法、依赖和差异检查通过。
- [x] OpenAPI 除标题外没有需求外契约变化。
- [x] `version.txt`、`.env` 和其他无关文件不进入本次变更。
- [x] 本次需求、设计、任务与三个索引状态一致。

**验证：**

```bash
.venv/bin/python -m compileall -q main.py app tools tests
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
.venv/bin/python -m pip check
git diff --check
git status --short
```

**可能修改文件：**

- `docs/spec/20260901-Tsi助手项目命名-需求.md`
- `docs/plan/20260901-Tsi助手项目命名-设计.md`
- `docs/tasks/20260901-Tsi助手项目命名-任务.md`
- `docs/spec/README.md`
- `docs/plan/README.md`
- `docs/tasks/README.md`

**规模：** S。

## Checkpoint 2：完成

- [x] 七项需求验收标准均有直接证据。
- [x] 完整门禁通过。
- [x] 差异可供人工审查，尚未自动创建 Commit。
