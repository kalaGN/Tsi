# TUI 美元符号引用技能任务

状态：已完成（2026-09-05）
创建日期：2026-09-05
对应设计：[`docs/plan/20260905-TUI美元符号引用技能-设计.md`](../plan/20260905-TUI美元符号引用技能-设计.md)

## 阶段一：引用解析与请求快照

### 任务 1：实现有界显式 Skill 引用解析

- [x] 在 `tools/skills.py` 中解析完整 `$技能名`，按 Catalog 存在性过滤并去重。
- [x] 拒绝超过 3 个有效引用或超过上下文总字节限制，未知名称保持普通文本语义。
- [x] 在 `tests/test_skills.py` 覆盖边界、去重、未知名称、数量和字节上限。
- 验收：解析不读磁盘、不加载资源正文，异常文案不含 Skill 正文或绝对路径。
- 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_skills.py`
- 依赖：无。
- 预计范围：S，2 个文件。

### 任务 2：让单次会话快照接收原始输入

- [x] 修改 `ExecutionSnapshotProvider` 契约，使 `ChatSession.send()` 每轮传入原始输入且只调用一次。
- [x] `SkillRuntime` 用同一 Catalog 生成显式 Skill system 数据和 Registry，保留无参数启动快照。
- [x] 更新 Session 与 SkillRuntime 测试，验证正文仅进入当前请求、原始输入仍持久化、安装版本隔离不变。
- 验收：普通请求系统提示词不变，超限错误发生在 Provider 调用前。
- 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat_session.py tests/test_skill_runtime.py`
- 依赖：任务 1。
- 预计范围：M，4 个文件。

## 检查点一

- [x] Runtime、Session、Skill 和工具测试通过。
- [x] HTTP `/chat` 契约测试通过。
- [x] Session 文件不包含 Skill 正文。

## 阶段二：输入候选交互

### 任务 3：增加独立 SkillPalette

- [x] 创建 `app/tui/skill_palette.py`，实现光标片段识别、候选过滤、循环选择、关闭和补全结果。
- [x] 在 TCSS 中增加与命令候选一致但 ID 独立的有限高度区域。
- [x] 增加纯解析和无头组件测试，覆盖多行、中文、光标中间、边界和空 Catalog。
- 验收：组件只使用内存摘要，不依赖 Runtime、磁盘或 TextArea 实例。
- 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui_skill_palette.py`
- 依赖：无。
- 预计范围：M，3 个文件。

### 任务 4：接入 TUI 键盘和请求流程

- [x] 在 `ChatTuiApp` 组合 SkillPalette，并在文本或光标变化时更新。
- [x] 按既定优先级接入 Enter、Tab、Up、Down、Esc，精确替换当前引用片段。
- [x] 增加端到端无头测试，验证与命令候选、历史、发送、Esc 和安装刷新兼容。
- 验收：补全不调用模型，第二次 Enter 才发送；普通中文输入和未知 `$名称` 可正常发送。
- 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py tests/test_tui_skill_palette.py`
- 依赖：任务 2、任务 3。
- 预计范围：M，3 个文件。

## 检查点二

- [x] `$` 候选到显式 Skill system 上下文的端到端路径通过。
- [x] `/clear`、`/skills`、`/quit` 和命令预览全部回归通过。
- [x] 写文件与脚本仍逐次审批，HTTP 未加载 Skill。

## 阶段三：文档与交付

### 任务 5：同步项目文档并完成质量门禁

- [x] 更新 README 使用示例、AGENTS 架构索引和架构 Knowledge。
- [x] 将需求、设计和任务状态更新为已完成，并勾选真实完成项。
- [x] 执行语法、全量测试、依赖和差异检查，进行五维代码审查。
- 验收：文档明确 `$` 仅显式加载 Skill，不会自动执行脚本或批准工具。
- 验证：执行设计文档第 9 节全部命令。
- 依赖：任务 4。
- 预计范围：M，5 个文件。

## 最终检查点

- [x] 全部验收标准通过。
- [x] 没有新增第三方依赖、Session 迁移或 HTTP 行为变化。
- [x] 未提交文件范围明确，等待用户要求后再创建 Git Commit。
