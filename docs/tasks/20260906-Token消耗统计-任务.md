# Token 消耗统计任务

状态：已完成（2026-09-06）
创建日期：2026-09-06
对应需求：[Token 消耗统计需求](../spec/20260906-Token消耗统计-需求.md)
对应设计：[Token 消耗统计设计](../plan/20260906-Token消耗统计-设计.md)

## 依赖顺序

```text
任务 1
  -> 任务 2 -> 任务 3
                 -> 任务 4 -> 任务 5 -> 任务 6 -> 任务 7 -> 任务 8
                                                        -> 任务 9 -> 任务 10
```

所有任务串行执行。任务 2 和任务 3 共用 Provider 测试文件；任务 4 和任务 5 共享日志调用契约，不安排并行修改。

## 阶段一：统一契约与 Provider

- [x] 任务 1：测试先行增加统一 Token usage 值对象。
  - 说明：先锁定三项字段、不变量、零值与加法，再让 `ModelStep` 以默认 `None` 携带单步骤 usage。
  - 验收：合法值和逐字段相加正确；负数、布尔值、非整数及总量不一致被拒绝；既有三参数 `ModelStep` 构造继续可用。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_contracts.py tests/test_llm_providers.py`
  - 依赖：无。
  - 文件：`app/services/llm/contracts.py`、`tests/test_llm_contracts.py`。
  - 规模：S（2 个文件）。

- [x] 任务 2：测试先行解析 DeepSeek 流式 usage。
  - 说明：请求 `include_usage`，将 `prompt_tokens/completion_tokens/total_tokens` 映射为统一值，并覆盖 usage-only Chunk。
  - 验收：文本和工具步骤均能提取 usage；null/缺失保持兼容；重复、空事件或非法 usage 映射为无效响应。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_providers.py -k deepseek`
  - 依赖：任务 1。
  - 文件：`app/services/llm/deepseek.py`、`tests/test_llm_providers.py`。
  - 规模：S（2 个文件）。

- [x] 任务 3：测试先行解析阿里云 completed usage。
  - 说明：从 `response.completed.response.usage` 映射三项统一字段，并让文本与工具步骤共用同一解析路径。
  - 验收：文本和工具步骤均携带 usage；null/缺失不影响原回答；非法字段、类型、范围及求和关系被拒绝。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_providers.py -k aliyun`
  - 依赖：任务 1、2。
  - 文件：`app/services/llm/aliyun.py`、`tests/test_llm_providers.py`。
  - 规模：S（2 个文件）。

## 检查点一

- [x] 两家 Provider 对相同语义输出相同 `TokenUsage`。
- [x] Provider 私有字段没有进入 Runtime、TUI 或 HTTP。
- [x] Provider 定向测试通过且未调用真实接口。

## 阶段二：日志与运行时聚合

- [x] 任务 4：测试先行增加逐模型步骤 Token 日志。
  - 说明：扩展事件白名单、stderr JSON 和本地中文 Formatter，提供只接收有限整数元数据的日志函数。
  - 验收：事件包含 request ID、步骤编号和三项 Token；中文与 JSON 格式稳定；不记录正文、原始 usage 或凭据。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_model_logging.py`
  - 依赖：任务 3。
  - 文件：`app/observability/model_logging.py`、`tests/test_model_logging.py`。
  - 规模：S（2 个文件）。

- [x] 任务 5：测试先行实现 Tool Loop 聚合和 Runtime 透传。
  - 说明：增加 `ToolLoopResult`，每步先记录 usage 再累计；让 `ChatResult` 以默认字段透传完整本轮统计。
  - 验收：单步和多工具步骤累计正确；任一步缺失则聚合不可用；后续失败或工具超限不删除已完成步骤日志。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tool_loop.py tests/test_chat_runtime.py tests/test_chat_session.py`
  - 依赖：任务 4。
  - 文件：`app/runtime/tool_loop.py`、`app/runtime/chat.py`、`tests/test_tool_loop.py`、`tests/test_chat_runtime.py`。
  - 规模：M（4 个文件）。

## 检查点二

- [x] 一个 request ID 下每个有 usage 的模型步骤恰好记录一次日志。
- [x] 只有全部步骤均有 usage 时才返回完整本轮合计。
- [x] 既有循环上限、审批、取消和 Session 原子提交测试通过。

## 阶段三：交互与公开契约

- [x] 任务 6：测试先行增加 TUI 本轮 Token 展示。
  - 说明：成功回答和耗时之后展示统一 Token 行；不可用、失败、取消及陈旧 Worker 使用明确边界。
  - 验收：成功显示输入/输出/合计，缺失显示“不可用”；失败和取消不显示伪造合计；Token 行不进入 Session 或输入历史。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py`
  - 依赖：任务 5。
  - 文件：`app/tui/application.py`、`tests/test_tui.py`。
  - 规模：S（2 个文件）。

- [x] 任务 7：验证 HTTP 契约和两家端到端接线。
  - 说明：让 HTTP Mock 响应包含真实形状 usage，并锁定内部统计不会进入成功 JSON 或 OpenAPI。
  - 验收：DeepSeek 与阿里云 `/chat` 均正常；响应仍严格等于 `{"output_text":"..."}`；错误映射不变。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat.py tests/test_llm_providers.py`
  - 依赖：任务 6。
  - 文件：`tests/test_chat.py`、`tests/test_llm_providers.py`。
  - 规模：S（2 个文件）。

## 检查点三

- [x] TUI 能展示真实本轮 Token，状态栏保持原样。
- [x] HTTP DTO 和 OpenAPI 不包含 usage。
- [x] 流式输出、耗时、工具调用和历史恢复无回归。

## 阶段四：文档与交付

- [x] 任务 8：同步使用说明和项目知识。
  - 说明：更新使用说明、架构和排障知识，记录真实统计、工具步骤累计和上下文估算的区别。
  - 验收：TUI 展示、日志事件、Provider 缺失行为和 HTTP 边界均与实际代码一致；不描述未实现的会话累计或费用。
  - 验证：搜索 README 和 Knowledge 中的 `usage`、`Token`、`上下文`，逐项核对实现。
  - 依赖：任务 7。
  - 文件：`README.md`、`docs/knowledge/architecture.md`、`docs/knowledge/troubleshooting.md`。
  - 规模：M（3 个文件）。

- [x] 任务 9：同步已确认需求和设计状态。
  - 说明：按真实实现修订需求与设计并更新对应索引，不提前把任务清单标为完成。
  - 验收：需求、设计、实现没有冲突；Spec 与 Design 索引准确标记已完成。
  - 验证：`git diff --check`。
  - 依赖：任务 8。
  - 文件：本需求、本设计、`docs/spec/README.md`、`docs/plan/README.md`。
  - 规模：M（4 个文件）。

- [x] 任务 10：执行全量门禁、五维审查并关闭任务。
  - 说明：完成正确性、可读性、架构、安全和性能审查；全部门禁通过后才更新任务及索引状态。
  - 验收：全量门禁通过且无阻断审查问题；任务复选框和索引只反映真实结果。
  - 验证：

    ```bash
    .venv/bin/python -m compileall -q main.py app tools tests
    PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
    .venv/bin/python -m pip check
    git diff --check
    git status --short
    ```

  - 依赖：任务 9。
  - 文件：本任务、`docs/tasks/README.md`。
  - 规模：S（2 个文件）。

## 最终检查点

- [x] 需求和设计的全部验收标准通过。
- [x] 两家 Provider、工具循环、日志、TUI 和 HTTP 形成可验证闭环。
- [x] 无新增依赖、无 Session schema 变化、无真实外部模型请求。
- [x] 未提交的其他功能和用户文件没有被覆盖或混入本任务判断。

确认结果：2026-09-06 用户回复“通过”，按当前依赖顺序开始实施。
