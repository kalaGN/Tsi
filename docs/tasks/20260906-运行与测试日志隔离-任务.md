# 运行与测试日志隔离任务

状态：已完成（2026-09-06）
创建日期：2026-09-06
对应需求：[运行与测试日志隔离需求](../spec/20260906-运行与测试日志隔离-需求.md)
对应设计：[运行与测试日志隔离设计](../plan/20260906-运行与测试日志隔离-设计.md)

## 阶段一：测试先行

- [x] 任务 1：增加默认日志路径契约测试。
  - 验收：测试明确要求运行与测试日志分别位于 `logs/runtime/` 和 `logs/tests/`，默认配置使用运行路径，显式 `log_path` 仍优先。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_model_logging.py`
  - 文件：`tests/test_model_logging.py`。

- [x] 任务 2：增加 Pytest 收集前日志隔离回归。
  - 验收：测试进程的预配置文件 Handler 指向 `logs/tests/model-calls.log`；后续导入或创建 FastAPI 应用不得切换到运行日志。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_logging_isolation.py`
  - 文件：`tests/conftest.py`、`tests/test_logging_isolation.py`。

## 阶段二：实现隔离

- [x] 任务 3：定义固定运行/测试路径并调整默认配置。
  - 验收：`configure_model_logging()` 默认写入 `RUNTIME_LOG_PATH`；Pytest 预配置写入 `TEST_LOG_PATH`；进程内仍只有一个文件 Handler。
  - 验证：任务 1、2 的定向测试全部通过。
  - 文件：`app/observability/model_logging.py`、`tests/conftest.py`。

## 检查点

- [x] 测试启动后文件 Handler 只指向测试目录。
- [x] HTTP/TUI 入口行为、stderr 策略和日志事件格式无变化。
- [x] 没有自动移动或删除旧日志。

## 阶段三：文档与交付

- [x] 任务 4：同步使用、架构和故障文档。
  - 验收：所有当前日志路径说明都区分 `runtime` 和 `tests`，并注明旧文件仅作历史保留。
  - 验证：搜索当前文档中的 `logs/model-calls.log`，逐项判断是否应更新。
  - 文件：`README.md`、`docs/knowledge/architecture.md`、`docs/knowledge/troubleshooting.md`、三类索引及本变更文档。

- [x] 任务 5：执行全量门禁并完成五维审查。
  - 验收：正确性、可读性、架构、安全和性能无阻断问题，任务状态只按真实验证结果更新。
  - 验证：

    ```bash
    PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
    .venv/bin/python -m compileall -q main.py app tools tests
    .venv/bin/python -m pip check
    git diff --check
    git status --short
    ```

  - 文件：所有本变更文件。

## 最终检查点

- [x] 需求和设计中的全部验收标准通过。
- [x] 测试日志可以与真实使用日志通过路径稳定区分。
- [x] 无新增依赖、无公开 API 变化、无真实外部模型请求。
