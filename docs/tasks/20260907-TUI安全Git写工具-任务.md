# TUI 安全 Git 写工具任务

状态：已完成（2026-09-08）

创建日期：2026-09-07

对应需求：[TUI 安全 Git 写工具需求](../spec/20260907-TUI安全Git写工具-需求.md)

对应设计：[TUI 安全 Git 写工具设计](../plan/20260907-TUI安全Git写工具-设计.md)

## Task 1：Git 审批与错误契约

- [x] 增加 Git 专属审批对象、固定风险文案、Registry 验证和 TUI 展示。
- [x] 非法审批对象不会进入 UI；Push 明确显示网络访问。
- [x] 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tools.py tests/test_tui.py`
- 文件：`tools/contracts.py`、`tools/registry.py`、`tools/__init__.py`、`app/tui/approval.py`、相关测试。

## Task 2：实现 Stage

- [x] 以临时 Index 生成零副作用预览，并在批准后重验、暂存明确文件。
- [x] 路径边界不能表达全仓库或删除；竞态返回 `git_conflict`。
- [x] 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_git_tools.py -k stage`
- 文件：`tools/git.py`、`tests/test_git_tools.py`；依赖 Task 1。

## Task 3：实现 Commit

- [x] 校验中文提交信息，审批 staged 快照并禁用 hooks/GPG 后提交。
- [x] 成功返回稳定短哈希；测试 hook 不执行。
- [x] 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_git_tools.py -k commit`
- 文件：`tools/git.py`、`tests/test_git_tools.py`；依赖 Task 2。

## Checkpoint 1：本地 Git 闭环

- [x] Stage 与 Commit 在临时仓库真实执行通过。
- [x] 拒绝、非法输入和竞态不会产生错误状态变更。
- [x] 工具输出和异常不包含绝对路径或 Git stderr。

## Task 4：实现 Push

- [x] 解析既有上游、脱敏目标并仅在批准后执行非强制 Push。
- [x] 无上游、无领先提交和不安全 URL 均拒绝；模型不能提供参数。
- [x] 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_git_tools.py -k push`
- 文件：`tools/git.py`、`tests/test_git_tools.py`；依赖 Task 1。

## Task 5：装配意图工具组

- [x] 把三项工具装配进 Workspace Catalog 和独立 `git_write` 组。
- [x] 激活后按固定顺序出现且仍独立审批；HTTP Registry 不变。
- [x] 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tool_groups.py tests/test_workspace_tools.py tests/test_chat.py`
- 文件：`tools/groups.py`、`tools/workspace.py`、相关测试；依赖 Task 2 至 Task 4。

## Task 6：文档与交付门禁

- [x] 同步工具表、架构、安全说明和故障排查。
- [x] Spec、设计和任务状态更新为已完成。
- [x] 执行编译、全量测试、依赖检查和 `git diff --check`。
- 文件：`README.md`、`docs/knowledge/`；依赖 Task 5。

## Checkpoint 2：交付

- [x] 全量测试通过，未删除或弱化既有断言。
- [x] Review 覆盖正确性、可读性、架构、安全和性能。
- [x] 无新增依赖、环境变量、HTTP 契约或持久化格式。
- [x] 未执行真实远程 Push、模型请求或用户仓库 Git 写操作。
- [x] 未经用户明确要求不提交、不推送。
