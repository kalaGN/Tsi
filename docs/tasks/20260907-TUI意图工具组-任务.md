# TUI 意图工具组任务

状态：已完成（2026-09-07）

创建日期：2026-09-07

对应需求：[TUI 意图工具组需求](../spec/20260907-TUI意图工具组-需求.md)

对应设计：[TUI 意图工具组设计](../plan/20260907-TUI意图工具组-设计.md)

## Task 1：建立中立动态工具契约

**说明：** 让 Runtime 依赖最小 Tool Runtime 协议，并让两个 Provider Turn 支持只影响下一步请求的工具定义替换。

**验收标准：**

- [x] 固定 `ToolRegistry` 自然满足新协议，行为不变。
- [x] DeepSeek 与阿里云 Turn 可在工具续轮之间替换定义，调用结果配对不丢失。
- [x] 静态 Registry 不触发无意义刷新。

**验证：**

- [x] Provider 单元测试覆盖首步和刷新后请求体。
- [x] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_providers.py tests/test_tool_loop.py`

**可能修改文件：**

- `tools/contracts.py`
- `app/services/llm/contracts.py`
- `app/services/llm/aliyun.py`
- `app/services/llm/deepseek.py`
- `app/runtime/tool_loop.py`

**依赖：** 无。

## Task 2：实现请求级 GroupedToolRegistry

**说明：** 实现固定组 Catalog、元工具、可见 Registry 重建和最多两次原子追加。

**验收标准：**

- [x] 初始定义只有 `activate_tool_groups`。
- [x] 单组、多组、包含、重复调用、未知组和激活上限符合设计。
- [x] 未激活工具不能执行，写工具激活后仍必须审批。

**验证：**

- [x] 新增 `tests/test_tool_groups.py` 覆盖状态、安全和错误。
- [x] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tool_groups.py tests/test_tools.py`

**可能修改文件：**

- `tools/groups.py`
- `tools/contracts.py`
- `tools/registry.py`
- `tools/__init__.py`
- `tests/test_tool_groups.py`

**依赖：** Task 1。

## Checkpoint 1：基础能力

- [x] 固定 Registry 全部测试仍通过。
- [x] 动态 Registry 只能发布构造时已有的宿主工具。
- [x] Provider 刷新不重建 Turn、不重复执行工具。

## Task 3：装配 Workspace 工具组

**说明：** 共享现有 Workspace 工具对象构造逻辑，并增加 TUI 专用意图 Registry 工厂，保持完整静态工厂兼容。

**验收标准：**

- [x] 五个组只在对应工具实际可用时发布。
- [x] `workspace_write` 包含全部只读和三项修改闭环工具。
- [x] 原 `create_workspace_registry` 定义集合和顺序不变。

**验证：**

- [x] Workspace 工厂测试覆盖有/无 Skill Catalog 和安装工具。
- [x] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_workspace_tools.py tests/test_skills.py tests/test_skill_installation.py`

**可能修改文件：**

- `tools/workspace.py`
- `tools/groups.py`
- `tools/__init__.py`
- `tests/test_workspace_tools.py`

**依赖：** Task 2。

## Task 4：接入 SkillRuntime 与 TUI

**说明：** 让每次发送获得全新的分组状态，显式 `$技能名` 预激活 Skill 组，并把新工厂接入 TUI 启动入口。

**验收标准：**

- [x] 普通请求初始只有激活工具。
- [x] `$技能名` 初始额外提供三项 Skill 工具，不提供安装工具。
- [x] 上一轮激活状态不会进入下一轮，安装热刷新语义不变。

**验证：**

- [x] Session、SkillRuntime 和 TUI 集成测试覆盖请求隔离与显式引用。
- [x] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_skill_runtime.py tests/test_chat_session.py tests/test_tui.py`

**可能修改文件：**

- `app/runtime/session.py`
- `app/runtime/chat.py`
- `app/runtime/skill_runtime.py`
- `app/tui/__main__.py`
- `tests/test_skill_runtime.py`

**依赖：** Task 3。

## Checkpoint 2：端到端行为

- [x] DeepSeek 和阿里云的真实请求形状由 Mock Transport 验证。
- [x] 激活步骤计入既有预算，达到边界仍返回 `tool_limit`。
- [x] HTTP `/chat` 相关测试全部不变并通过。
- [x] 所有副作用操作仍只能在审批成功后执行。

## Task 5：补齐端到端回归与文档

**说明：** 覆盖主要意图路径并同步项目架构、概览、README 和故障排查说明。

**验收标准：**

- [x] 普通、时间、只读、写入、Skill、安装和追加激活都有自动化覆盖。
- [x] 文档说明激活机制、组列表、Token 权衡、审批边界和回滚方式。
- [x] Spec、设计和任务状态更新为已完成。

**验证：**

- [x] `.venv/bin/python -m compileall -q main.py app tools tests`
- [x] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q`
- [x] `.venv/bin/python -m pip check`
- [x] `git diff --check`

**可能修改文件：**

- `tests/test_chat.py`
- `tests/test_llm_providers.py`
- `README.md`
- `docs/knowledge/architecture.md`
- `docs/knowledge/project-overview.md`

**依赖：** Task 4。

## Checkpoint 3：交付门禁

- [x] 完整测试通过，未弱化或删除既有断言。
- [x] 没有真实外部模型、网络、用户仓库或付费调用。
- [x] 没有新增依赖、环境变量、持久化格式或 HTTP 契约。
- [x] Review 覆盖正确性、可读性、架构、安全和性能。
- [x] 未经用户明确要求不提交、不推送。
