# TUI 模型切换任务

状态：已完成（2026-09-07）
创建日期：2026-09-07
对应需求：[TUI 模型切换需求](../spec/20260907-TUI模型切换-需求.md)
对应设计：[TUI 模型切换设计](../plan/20260907-TUI模型切换-设计.md)

## 依赖顺序

```text
任务 1 -> 任务 2 -> 任务 3 -> 任务 4 -> 任务 5 -> 任务 6 -> 任务 7 -> 任务 8
```

所有任务串行执行。工厂契约、Session 替换和选择器状态是 TUI 集成的前置条件；共享文件较多，不安排并行修改。

## 阶段一：模型契约与运行时切换

- [x] 任务 1：测试先行增加安全模型目录与显式 Provider 创建。
  - 说明：定义不含密钥的 `ModelOption`，解析两家 `*_MODELS` 候选、当前/默认模型和 Key 状态，并提供按明确供应商与模型创建 Provider 的工厂入口。
  - 验收：候选顺序、去重、每家 50 项上限、非法项忽略、当前/默认补齐和 Key 布尔状态符合设计；既有 `create_provider()` 与 HTTP 配置行为不变。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_llm_contracts.py tests/test_llm_providers.py`
  - 依赖：无。
  - 文件：`app/services/llm/contracts.py`、`app/services/llm/factory.py`、`tests/test_llm_contracts.py`、`tests/test_llm_providers.py`。
  - 规模：M（4 个文件）。

- [x] 任务 2：测试先行实现 Session Provider 原子替换。
  - 说明：为 `ChatSession` 增加只替换 Provider 的同步入口，并以发送锁状态阻止活动请求期间切换。
  - 验收：切换后下一轮使用新 Provider；历史、存储、系统提示词和工具快照不变；发送期间拒绝且原 Provider 保持不变。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_chat_session.py`
  - 依赖：任务 1。
  - 文件：`app/runtime/session.py`、`tests/test_chat_session.py`。
  - 规模：S（2 个文件）。

## 检查点一

- [x] 工厂候选不包含密钥正文，显式创建不会修改环境。
- [x] Session 切换只影响后续请求，不改变持久化格式。
- [x] HTTP 与现有 Provider 定向测试全部通过。

## 阶段二：命令与选择器组件

- [x] 任务 3：测试先行注册 `/model` 本地命令。
  - 说明：在唯一命令目录中增加 `/model` 和中文说明，保持完整命令解析及 `/m` 前缀预览行为。
  - 验收：目录无重复；`/model` 能解析；`/m` 只给出准确候选；带参数形式继续作为非本地命令处理。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui_commands.py`
  - 依赖：任务 2。
  - 文件：`app/tui/commands.py`、`tests/test_tui_commands.py`。
  - 规模：S（2 个文件）。

- [x] 任务 4：测试先行实现独立 ModelPalette。
  - 说明：新增有界模型候选组件，负责当前项定位、纯文本渲染、循环移动、确认和关闭，并补充有界布局样式。
  - 验收：当前项默认选中；上下循环；可用与 Key 缺失项均安全显示；确认返回准确对象；关闭不产生选择；大量候选不无限撑高界面。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui_model_palette.py`
  - 依赖：任务 3。
  - 文件：`app/tui/model_palette.py`、`app/tui/styles/application.tcss`、`tests/test_tui_model_palette.py`。
  - 规模：M（3 个文件）。

## 检查点二

- [x] `/model` 进入既有命令预览，但不接受直接参数切换。
- [x] ModelPalette 不读取环境、不创建 Provider、不操作 Session。
- [x] 候选文本不作为 Rich markup 或终端控制内容执行。

## 阶段三：TUI 端到端接入

- [x] 任务 5：测试先行接入 `/model` 完整交互。
  - 说明：在主应用装配 ModelPalette，按设计调整 Enter、Up/Down、Esc 优先级，并协调目录、Provider 创建、Session 替换、状态栏和 System 消息。
  - 验收：列表可打开、导航、确认和取消；成功切换更新状态栏并影响下一轮；Key 缺失或创建失败不切换；命令不进入输入历史、Session 或模型调用。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_tui.py tests/test_tui_commands.py tests/test_tui_model_palette.py`
  - 依赖：任务 4。
  - 文件：`app/tui/application.py`、`tests/test_tui.py`。
  - 规模：S（2 个文件）。

## 检查点三

- [x] 当前模型默认选中，上下键循环、回车确认、Esc 取消与 Codex 风格目标一致。
- [x] `/clear`、`/skills`、`/quit`、Skill 补全、输入历史、审批和双 Esc 行为无回归。
- [x] 切换前后的 Session 历史和 JSON schema 保持一致。

## 阶段四：文档与交付

- [x] 任务 6：同步配置说明和项目知识。
  - 说明：记录 `/model` 用法、两项候选环境变量、进程级切换范围、历史保留和错误排查，并同步当前架构事实。
  - 验收：文档与真实候选顺序、限制、Key 缺失行为和 HTTP 边界一致；示例不包含密钥或承诺外部模型可用。
  - 验证：搜索 `DEEPSEEK_MODELS`、`ALIYUN_MODELS`、`/model` 并逐项核对实现，随后运行 `git diff --check`。
  - 依赖：任务 5。
  - 文件：`README.md`、`docs/knowledge/project-overview.md`、`docs/knowledge/architecture.md`、`docs/knowledge/troubleshooting.md`。
  - 规模：M（4 个文件）。

- [x] 任务 7：同步需求与设计状态。
  - 说明：按真实实现修订需求和设计，并更新 Spec、Design 索引；任务清单暂不提前标记完成。
  - 验收：需求、设计和实现一致；所有改变的决策都有记录；两个索引准确反映已完成状态。
  - 验证：`git diff --check`。
  - 依赖：任务 6。
  - 文件：本需求、本设计、`docs/spec/README.md`、`docs/plan/README.md`。
  - 规模：M（4 个文件）。

- [x] 任务 8：执行全量门禁、五维审查并关闭任务。
  - 说明：完成正确性、可读性、架构、安全和性能审查；只有全部通过后才更新任务复选框和任务索引。
  - 验收：九项需求验收标准全部有测试或文档证据；无阻断审查问题；未混入其他未提交需求。
  - 验证：

    ```bash
    .venv/bin/python -m compileall -q main.py app tools tests
    PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q
    .venv/bin/python -m pip check
    git diff --check
    git status --short
    ```

  - 依赖：任务 7。
  - 文件：本任务、`docs/tasks/README.md`。
  - 规模：S（2 个文件）。

## 最终检查点

- [x] `/model` 候选列表、键盘交互、Provider 切换与状态展示形成可验证闭环。
- [x] 候选配置有界、无密钥泄漏、无网络目录请求且无新增依赖。
- [x] HTTP `/chat`、会话持久化、工具与 Skill 权限边界没有变化。
- [x] 全量测试及四项门禁通过，文档状态反映真实实现。

确认结果：2026-09-07 用户回复“go on”，按已确认需求与设计完成全部任务。
