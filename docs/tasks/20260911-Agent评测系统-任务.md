# Agent 评测系统任务

状态：已确认（2026-09-11）

对应需求：[Agent 评测系统需求](../spec/20260911-Agent评测系统-需求.md)

对应设计：[Agent 评测系统设计](../plan/20260911-Agent评测系统-设计.md)

## 阶段一：契约与确定性 Provider

- [x] 任务 1：实现评测 Case、Setup、Expected、回放步骤和结果数据契约，并严格加载 JSONL。
  - 验收：支持设计中的全部字段；拒绝未知字段、重复 ID、非法历史、危险路径、超限文本和错误类型；错误包含文件、行号与 Case ID。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/evaluation/test_contracts.py`
  - 文件：`app/evaluation/__init__.py`、`app/evaluation/contracts.py`、`tests/evaluation/test_contracts.py`

- [x] 任务 2：实现符合现有 LLM 契约的回放 Provider。
  - 验收：按 Turn/Step 返回文本、工具调用与 Token；记录收到的消息和工具；支持最终文本增量；步骤不足或剩余脚本显式失败；无网络访问。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/evaluation/test_replay.py`
  - 文件：`app/evaluation/replay.py`、`tests/evaluation/test_replay.py`

## 阶段二：真实 Runtime 轨迹

- [x] 任务 3：增加 Provider 中立的 Runtime Trace 契约，并贯通 Chat Runtime 与 Session。
  - 验收：请求开始、成功、失败事件完整；`ChatSession.send()` 可传递 Observer；不传 Observer 时公开行为和错误映射不变。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/evaluation/test_runtime_trace.py tests/test_chat_runtime.py tests/test_chat_session.py`
  - 文件：`app/runtime/trace.py`、`app/runtime/chat.py`、`app/runtime/session.py`、`tests/evaluation/test_runtime_trace.py`

- [x] 任务 4：在真实 Tool Loop 记录模型步骤、可见工具、工具调用、审批、结果、耗时和 Token。
  - 验收：动态工具组前后可见列表准确；拒绝、错误、超限轨迹可定位；现有日志、调用顺序和限制不变。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/evaluation/test_tool_trace.py tests/test_tool_loop.py`
  - 文件：`app/runtime/tool_loop.py`、`app/runtime/trace.py`、`tests/evaluation/test_tool_trace.py`

## 阶段三：隔离执行与轨迹收集

- [x] 任务 5：实现递归脱敏、Trace Collector 和 Harness 指纹。
  - 验收：密钥字段、Bearer 文本、绝对路径和超长内容不会进入报告对象；AGENTS、Skills、工具、策略和 Git 状态指纹稳定且不含正文或绝对路径。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/evaluation/test_observer.py tests/evaluation/test_fingerprint.py`
  - 文件：`app/evaluation/observer.py`、`app/evaluation/fingerprint.py`、`tests/evaluation/test_observer.py`、`tests/evaluation/test_fingerprint.py`

- [x] 任务 6：实现临时 Case 环境和 Runner，复用真实 ChatSession、SkillRuntime、工具组及审批。
  - 验收：每个 Trial 独立复制 Harness、写入 Setup、持久化独立 Session、执行真实链路、断言前捕获文件快照并始终清理；默认 replay，live 必须显式配置。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/evaluation/test_environment.py tests/evaluation/test_runner.py`
  - 文件：`app/evaluation/environment.py`、`app/evaluation/runner.py`、`tests/evaluation/test_environment.py`、`tests/evaluation/test_runner.py`

## 阶段四：评分、报告和命令

- [x] 任务 7：实现确定性断言、四维评分、多 Trial 聚合和安全失败规则。
  - 验收：文本、上下文、工具、审批、状态、文件、Token、耗时断言均有证据；空维度权重正确归一化；安全失败强制 Trial 失败。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/evaluation/test_graders.py`
  - 文件：`app/evaluation/graders.py`、`app/evaluation/contracts.py`、`tests/evaluation/test_graders.py`

- [x] 任务 8：实现 JSON/中文 Markdown 报告、基线比较和 `run`/`compare` CLI。
  - 验收：报告原子写入日期前缀文件；退出码符合设计；模式与模型可比性、3 分/5 个百分点/安全回归规则准确；报告目录被 Git 忽略。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/evaluation/test_report.py tests/evaluation/test_cli.py`
  - 文件：`app/evaluation/report.py`、`app/evaluation/__main__.py`、`tests/evaluation/test_report.py`、`tests/evaluation/test_cli.py`、`.gitignore`

- [x] 任务 9：实现可选独立 LLM Judge。
  - 验收：Judge 不开放工具，只接受严格有限 JSON；生成 `-judged` 新报告；单项失败继续、全部失败退出码 3；Judge Token 与 Agent Token 分开且不能覆盖确定性安全结论。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/evaluation/test_llm_judge.py tests/evaluation/test_cli.py`
  - 文件：`app/evaluation/llm_judge.py`、`app/evaluation/__main__.py`、`app/evaluation/report.py`、`tests/evaluation/test_llm_judge.py`

## 阶段五：核心 Suite 与交付

- [x] 任务 10：编写九类核心回放 Case 和初始回放基线。
  - 验收：覆盖直答、只读工具、写批准、写拒绝、工具错误恢复、循环上限、按需工具组、显式 Skill、记忆上下文；核心 Suite 在断网环境通过；基线由该通过报告生成且标明当前 Harness 指纹。
  - 验证：`.venv/bin/python -m app.evaluation run --suite evals/cases/core.jsonl`
  - 文件：`evals/cases/core.jsonl`、`evals/baselines/core.json`、`tests/evaluation/test_core_suite.py`

- [x] 任务 11：同步用户文档、架构知识和索引。
  - 验收：README 提供回放、live、Judge、报告与基线示例；架构文档说明 Trace 依赖方向、隔离边界和数据生命周期；文档均为中文且文件名符合日期规则。
  - 验证：`git diff --check`
  - 文件：`README.md`、`docs/knowledge/architecture.md`、`docs/knowledge/project-overview.md`、`docs/spec/README.md`、`docs/plan/README.md`

- [x] 任务 12：运行完整门禁并执行交付前代码审查。
  - 验收：核心回放报告通过；编译、全量测试、依赖和 Diff 检查通过；审查发现的问题已修复；需求、设计和任务状态更新为已完成。
  - 验证：
    - `.venv/bin/python -m app.evaluation run --suite evals/cases/core.jsonl`
    - `.venv/bin/python -m compileall -q main.py app tools tests`
    - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q`
    - `.venv/bin/python -m pip check`
    - `git diff --check`
  - 文件：`docs/spec/20260911-Agent评测系统-需求.md`、`docs/plan/20260911-Agent评测系统-设计.md`、`docs/tasks/20260911-Agent评测系统-任务.md`、对应索引及审查修复涉及文件

## 约束

- 按任务编号顺序实施，每项先补失败测试再写实现。
- 自动化验证不得调用真实模型、外部网络、Skill 脚本、Git Push 或生产服务。
- 不得为通过评测降低工具审批、Workspace Policy、循环限制或既有测试强度。
- 未经用户明确要求不执行 Git Commit。
