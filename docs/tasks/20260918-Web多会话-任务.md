# Web 多会话任务

状态：已完成（2026-09-18）

对应需求：[Web 多会话需求](../spec/20260918-Web多会话-需求.md)

对应设计：[Web 多会话设计](../plan/20260918-Web多会话-设计.md)

## 阶段一：持久化基础

- [x] 任务 1：实现会话索引、独立文件和旧数据迁移。
  - 验收：创建、选择、排序、重命名、删除、最后会话回退和迁移均原子持久化；非法索引失败关闭。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_web_sessions.py`
  - 文件：`app/webui/sessions.py`、`tests/test_web_sessions.py`

## 阶段二：后端用例与 API

- [x] 任务 2：把 `WebUiService` 改为多会话编排并扩展路由。
  - 验收：Bootstrap、创建、切换、重命名、清空和删除返回一致 DTO；忙碌时返回 409；流事件绑定会话。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_webui.py`
  - 文件：`app/webui/service.py`、`app/webui/router.py`、`tests/test_webui.py`

### 检查点

- [x] 两组定向测试通过。
- [x] `/chat` 与 TUI 契约未改动。

## 阶段三：页面交互

- [x] 任务 3：实现真实会话列表及全部操作。
  - 验收：页面可新建、切换、重命名、清空、删除；自动标题与排序及时更新；窄屏切换收起侧栏。
  - 验证：静态契约测试和真实浏览器手工验收。
  - 文件：`app/webui/static/index.html`、`app/webui/static/app.js`、`app/webui/static/app.css`、`tests/test_webui.py`

## 阶段四：文档与交付

- [x] 任务 4：同步 README、架构知识和原 Web UI 规范中的单会话描述。
  - 验收：启动、存储、迁移、API 与非目标说明一致，无相互冲突文案。
  - 验证：`git diff --check`
  - 文件：`README.md`、`docs/knowledge/architecture.md`、`docs/knowledge/project-overview.md`、相关 Spec/Plan/Tasks 与索引。

- [x] 任务 5：执行完整门禁与浏览器验收。
  - 验收：完整测试、离线评测、编译、依赖、Diff 和浏览器多会话流程通过。
  - 验证：项目完整交付命令。
  - 文件：本次变更全部文件。

## 验证结果

- 自动化：`615 passed`；核心离线评测 9/9、100 分；编译、`pip check` 与 `git diff --check` 通过。
- 浏览器：创建两段不同对话、自动标题、切换和刷新恢复通过；重命名、清空和删除由 Service/API 自动化测试覆盖。
