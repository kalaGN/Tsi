# Web UI 与 macOS 桌面应用任务

> 后续状态：Web 审批 Broker 与页面交互由 [20260919-Web工作区写入任务](20260919-Web工作区写入-任务.md) 单独实施；桌面 Host 与打包任务仍按本文状态管理。

状态：Web UI 第一阶段已完成（2026-09-18）；macOS 壳待后续确认

对应需求：[macOS 桌面应用需求](../spec/20260918-mac桌面应用-需求.md)

对应设计：[macOS 桌面应用设计](../plan/20260918-mac桌面应用-设计.md)

## 阶段一：Web UI 基础与安全连接

- [x] 任务 1：在现有 FastAPI 增加延迟装配的 `/ui` 页面与本机专属 API。
  - 验收：普通 HTTP 入口不因 Web 状态而在导入时失败；页面与 API 拒绝非 loopback 客户端。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_webui.py tests/test_chat.py`
  - 文件：`app/application.py`、`app/webui/__init__.py`、`app/webui/router.py`、`tests/test_webui.py`

- [x] 任务 2：实现独立 Web 会话、流式 NDJSON、取消、清理和模型切换。
  - 验收：只在完整成功后保存 Web 历史；事件有请求 ID 和递增序号；一次只运行一个请求；不覆盖 TUI 会话。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_webui.py tests/test_chat_session.py tests/test_model_selection.py`
  - 文件：`app/webui/service.py`、`app/webui/router.py`、`tests/test_webui.py`

- [x] 任务 3：增加只读 Workspace 工具组、文件树和文本预览。
  - 验收：Web 模型和页面都不能获得编辑、删除、脚本、安装或 Git 写工具；保护路径和二进制仍被拒绝。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_webui.py tests/test_workspace_tools.py tests/test_tool_groups.py`
  - 文件：`tools/workspace.py`、`app/webui/service.py`、`app/webui/router.py`、`tests/test_webui.py`

- [x] 任务 4：实现 DSH 风格响应式三栏页面。
  - 验收：中文输入、流式文本、安全 Markdown、发送/停止、模型选择、主题、文件/活动面板和窄屏收折可用；不加载远程资源。
  - 验证：静态安全契约测试和浏览器手工验收。
  - 文件：`app/webui/static/index.html`、`app/webui/static/app.css`、`app/webui/static/app.js`、`tests/test_webui.py`

## 阶段二：Web UI 文档与交付

- [x] 任务 5：同步 README、架构知识并完成全量验证和浏览器验收。
  - 验收：中文启动与安全边界文档已同步；真实浏览器通过浅色/深色、三栏首屏与文件预览验收；完整测试和离线评测通过。
  - 验证：`603 passed`；核心评测 9/9、100 分；编译、`pip check` 与 `git diff --check` 通过。

- [x] 任务 5.1：增加 Web UI 设置面板和本地界面偏好。
  - 验收：可设置跟随系统/浅色/深色主题、舒适/紧凑密度和发送快捷键；模型选择与顶栏同步；界面偏好仅保存在浏览器。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_webui.py`
  - 文件：`app/webui/static/index.html`、`app/webui/static/app.css`、`app/webui/static/app.js`、`tests/test_webui.py`

- [x] 任务 5.2：增加 Web UI 持久化多会话和旧数据迁移。
  - 验收：新建、切换、重命名、清空、删除、自动标题和刷新恢复可用；会话内容隔离且索引原子写入。
  - 验证：`615 passed`；核心离线评测 9/9、100 分；浏览器创建、切换和刷新恢复通过。
  - 文件：`app/webui/sessions.py`、`app/webui/service.py`、`app/webui/router.py`、`app/webui/static/`、`tests/test_web_sessions.py`、`tests/test_webui.py`

## 后续阶段：macOS Host 与安全连接

- [ ] 任务 6：增加桌面依赖、延迟导入入口、loopback 随机端口和可停止 Host。
  - 验收：无图形环境仍可导入现有应用；Host 只监听回环地址，健康后再打开窗口，退出完成清理。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/desktop/test_host.py`
  - 文件：`requirements.txt`、`app/desktop/__init__.py`、`app/desktop/__main__.py`、`app/desktop/host.py`、`tests/desktop/test_host.py`

- [ ] 任务 7：实现桌面应用组装、静态资源和启动令牌交换。
  - 验收：无令牌 API 被拒绝；令牌只在根 URL 使用一次并换取 HttpOnly Cookie；Host/Origin 非本机请求被拒绝。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/desktop/test_application.py`
  - 文件：`app/desktop/application.py`、`app/desktop/api.py`、`app/desktop/contracts.py`、`app/desktop/static/index.html`、`tests/desktop/test_application.py`

## 后续阶段：桌面状态复用与模型

- [ ] 任务 3：让桌面 Host 复用现有 Web 会话索引、独立 SessionStore 和会话 API。
  - 验收：至少 20 个会话可持久化恢复；路径和消息隔离；索引原子写入且权限为 `0600`；不覆盖 TUI 会话。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/desktop/test_sessions.py`
  - 文件：`app/desktop/sessions.py`、`app/desktop/contracts.py`、`tests/desktop/test_sessions.py`

- [ ] 任务 4：复用模型目录、切换和持久化能力。
  - 验收：仅返回脱敏候选；缺少 Key 的模型不可选择；切换作用于后续请求并在重启后恢复。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/desktop/test_models.py tests/test_model_selection.py tests/test_model_selection_store.py`
  - 文件：`app/desktop/api.py`、`app/desktop/sessions.py`、`tests/desktop/test_models.py`

## 阶段三：流式对话与审批

- [ ] 任务 5：实现有序 NDJSON 流、发送、取消、错误、Token、耗时和上下文事件。
  - 验收：复用真实 ChatSession；断开或取消不保存半截回答；每个事件具备请求 ID 和递增序号；一次只运行一个请求。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/desktop/test_stream.py`
  - 文件：`app/desktop/api.py`、`app/desktop/sessions.py`、`app/desktop/contracts.py`、`tests/desktop/test_stream.py`

- [ ] 任务 6：实现 ApprovalBroker 并接入现有工具审批。
  - 验收：审批默认拒绝、只能决议一次；页面不能修改工具参数；拒绝、超时、断开和取消均不执行写操作。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/desktop/test_approvals.py`
  - 文件：`app/desktop/approvals.py`、`app/desktop/api.py`、`app/desktop/sessions.py`、`tests/desktop/test_approvals.py`

- [ ] 任务 7：实现安全文件树和文本预览。
  - 验收：与 Workspace Policy 一致地拒绝越界、保护路径、链接、二进制和超限文件；只返回有限文本和相对路径。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/desktop/test_files.py tests/test_workspace_tools.py`
  - 文件：`app/desktop/api.py`、`app/desktop/contracts.py`、`tests/desktop/test_files.py`

## 阶段四：DSH 风格页面

- [ ] 任务 8：实现响应式三栏壳、会话列表、欢迎态和主题。
  - 验收：左栏和右栏可收起；900px 以下对话区保持可用；支持跟随系统、浅色和深色；无远程资源。
  - 验证：DOM 契约测试和 macOS 手工验收。
  - 文件：`app/desktop/static/index.html`、`app/desktop/static/app.css`、`app/desktop/static/app.js`、`tests/desktop/test_static_ui.py`

- [ ] 任务 9：实现消息、流式 Markdown、输入、停止、模型选择和会话操作。
  - 验收：中文可正常输入；消息可选复制；Markdown 不执行 HTML/脚本；发送、取消、切换和恢复状态准确。
  - 验证：前端状态单元契约测试和 macOS 手工验收。
  - 文件：`app/desktop/static/index.html`、`app/desktop/static/app.css`、`app/desktop/static/app.js`、`tests/desktop/test_static_ui.py`

- [ ] 任务 10：实现右侧文件、活动和审批交互。
  - 验收：文件预览和工具活动不泄露完整敏感内容；审批默认聚焦拒绝；相同审批不能重复提交。
  - 验证：API/DOM 契约测试和批准、拒绝手工验收。
  - 文件：`app/desktop/static/index.html`、`app/desktop/static/app.css`、`app/desktop/static/app.js`、`tests/desktop/test_static_ui.py`

## 阶段五：打包、文档和交付

- [ ] 任务 11：增加 PyInstaller 配置和 macOS 构建脚本。
  - 验收：脚本生成未签名 `Tsi 助手.app`，包含静态资源且可启动；构建产物被忽略。
  - 验证：`scripts/build_macos_app.sh` 和本机启动冒烟。
  - 文件：`packaging/tsi-desktop.spec`、`scripts/build_macos_app.sh`、`.gitignore`

- [ ] 任务 12：同步 README、架构知识和三类索引。
  - 验收：中文说明开发启动、构建、配置、安全、已知限制和 DSH 仅为设计参考；不承诺签名或官方关联。
  - 验证：`git diff --check`
  - 文件：`README.md`、`docs/knowledge/architecture.md`、`docs/knowledge/project-overview.md`、`docs/spec/README.md`、`docs/plan/README.md`、`docs/tasks/README.md`

- [ ] 任务 13：执行完整门禁和 macOS 手工验收。
  - 验收：桌面测试、全量测试、离线核心评测、编译、依赖和 Diff 检查通过；需求、设计和任务状态更新为已完成。
  - 验证：
    - `.venv/bin/python -m app.evaluation run --suite evals/cases/core.jsonl`
    - `.venv/bin/python -m compileall -q main.py app tools tests`
    - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q`
    - `.venv/bin/python -m pip check`
    - `git diff --check`
  - 文件：本变更全部文件及审查修复涉及文件。

## 约束

- 未确认前不修改生产代码和依赖。
- 自动化测试不得打开真实窗口、调用真实模型、访问外部网络或批准真实 Git Push。
- 桌面端不得绕过现有 Workspace Policy、工具白名单、Skill 权限和逐次审批。
- 未经用户明确要求不执行 Git Commit。
