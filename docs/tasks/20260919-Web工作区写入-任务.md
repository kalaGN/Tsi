# Web 工作区写入任务

状态：已完成（2026-09-19）

对应需求：[Web 工作区写入需求](../spec/20260919-Web工作区写入-需求.md)

对应设计：[Web 工作区写入设计](../plan/20260919-Web工作区写入-设计.md)

## 阶段一：安全工具集合与审批协调

- [x] 任务 1：增加不含 Git、Skill 和脚本能力的 Web 写入专用工具 Registry。
  - 验收：可按意图激活 `workspace_write`；固定工具名与需求一致；初始只披露激活工具。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_workspace_tools.py`
  - 文件：`tools/workspace.py`、`tests/test_workspace_tools.py`

- [x] 任务 2：实现一次性、请求绑定的 Web 审批协调器。
  - 验收：批准、拒绝、请求冲突、重复、过期和取消清理均失败关闭。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_web_approvals.py`
  - 文件：`app/webui/approvals.py`、`tests/test_web_approvals.py`

## 阶段二：Service 与 API

- [x] 任务 3：把审批回调接入 Web 流式请求并增加决策 API。
  - 验收：批准前不落盘，批准后执行，拒绝不执行；取消和断流清理；会话门禁不变。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_webui.py`
  - 文件：`app/webui/service.py`、`app/webui/router.py`、`tests/test_webui.py`

### 检查点

- [x] 写操作只有通过当前待审批 Future 才能执行。
- [x] Bootstrap 不暴露内部审批状态，能力字段与实际一致。

## 阶段三：Web 交互

- [x] 任务 4：增加审批弹窗、流事件处理和安全状态清理。
  - 验收：Diff 可滚动复制，默认拒绝，Esc 拒绝，按钮只提交一次，终止请求关闭弹窗，成功写入后刷新文件列表。
  - 验证：静态契约测试与浏览器手工验证。
  - 文件：`app/webui/static/index.html`、`app/webui/static/app.js`、`app/webui/static/app.css`、`tests/test_webui.py`

## 阶段四：文档、审查与交付

- [x] 任务 5：同步 README、架构知识和三份变更文档状态。
  - 验收：权限、API、审批、安全与启动说明无冲突。
  - 验证：`git diff --check`
  - 文件：`README.md`、`docs/knowledge/architecture.md`、需求/设计/任务及索引。

- [x] 任务 6：执行完整测试和代码审查。
  - 验收：定向测试、完整测试、编译、依赖、Diff 检查和真实浏览器流程全部通过，无高优先级审查问题。
  - 验证：需求文档第 6 节全部命令。
  - 文件：本次变更全部文件。

## 验证结果

- `tests/test_workspace_tools.py`：56 通过。
- Web 审批、Web UI 与 Workspace 定向测试：77 通过。
- 完整测试：623 通过。
- Python 编译、依赖完整性和 Git Diff 格式检查通过。
- 真实浏览器页面加载成功，审批控件存在，浏览器控制台无警告或错误。
