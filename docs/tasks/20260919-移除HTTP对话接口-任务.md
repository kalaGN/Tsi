# 移除 HTTP 对话接口任务

状态：已完成（2026-09-19）

对应需求：[移除 HTTP 对话接口需求](../spec/20260919-移除HTTP对话接口-需求.md)

对应设计：[移除 HTTP 对话接口设计](../plan/20260919-移除HTTP对话接口-设计.md)

- [x] 任务 1：删除路由层并从应用注册中移除。
  - 验收：`POST /chat` 返回 404；`GET /` 与 `/ui` 不受影响；Runtime 与 Provider 契约不变。
  - 文件：`app/routers/`（删除）、`app/application.py`、`app/runtime/chat.py`（仅模块说明）。

- [x] 任务 2：收敛契约测试。
  - 验收：只保留应用装配与已移除端点测试，无遗留 `app.routers` 引用。
  - 文件：`tests/test_chat.py`（删除）、`tests/test_application.py`（新增）。

- [x] 任务 3：同步当前状态文档并执行完整门禁。
  - 验收：README、AGENTS、Knowledge 和 Rules 不再把 HTTP 描述为独立对话入口。
  - 文件：`README.md`、`AGENTS.md`、`docs/knowledge/`、`docs/rules/`。

## 验证结果

- 完整测试：613 通过。删除 31 个 HTTP 契约用例（22 个测试函数，其中 3 个参数化展开为 13 个实例），新增 1 个移除断言。
- Python 编译、依赖完整性和 Git Diff 格式检查通过。
- `POST /chat` 返回 404，OpenAPI `paths` 不含 `/chat`。
- Web UI 与 TUI 的测试未做任何修改，全部继续通过。
