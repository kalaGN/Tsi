# Web 设置与统计任务

状态：已完成（2026-09-20）

- [x] 任务 1：实现有界 Web 统计 Store。
  - 验收：成功、失败、取消、Usage、耗时、日桶和模型桶聚合正确；文件原子持久化、损坏可隔离、敏感字段不进入 DTO。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_web_statistics.py`
  - 文件：`app/webui/statistics.py`、`tests/test_web_statistics.py`。

- [x] 任务 2：接入请求生命周期与统计 API。
  - 验收：每个已接受请求只记录一个终态，统计失败不改变流式结果，`GET /ui/api/statistics` 受 loopback 保护并返回稳定 DTO。
  - 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q tests/test_web_statistics.py tests/test_webui.py`
  - 文件：`app/webui/service.py`、`app/webui/router.py`、`tests/test_webui.py`。

- [x] 任务 3：将设置弹窗改为独立页面并增加统计波形图。
  - 验收：Hash 路由、左侧菜单、通用/模型/统计页面、返回对话和草稿保留正常；波形图可切换 7/30 天及请求数/Token/平均耗时。
  - 验证：Web 静态契约测试，以及浏览器手工检查桌面和窄屏布局。
  - 文件：`app/webui/static/index.html`、`app/webui/static/app.css`、`app/webui/static/app.js`、`tests/test_webui.py`。

- [x] 任务 4：同步文档并执行交付门禁。
  - 验收：需求、设计、任务和用户文档与实现一致；完成正确性、可读性、架构、安全和性能审查；定向与完整门禁全部通过。
  - 验证：
    - `.venv/bin/python -m compileall -q main.py app tools tests`
    - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q`
    - `.venv/bin/python -m pip check`
    - `git diff --check`
  - 文件：`README.md`、`docs/knowledge/`、三阶段文档和索引（按实际公开行为更新）。

## 可观测性问题

- 当前 Web 请求的成功、失败、取消比例是多少？由累计和日桶 outcome 统计回答。
- 哪个 Provider/模型承担了调用量和 Token？由有界模型桶回答。
- 最近 7 天或 30 天的调用量、Token 和平均耗时如何变化？由日桶和波形图回答。
- 统计自身是否发生损坏或持久化失败？由稳定、无正文的 Store 错误日志回答。

## 验证记录

- Store、Web 生命周期、API、静态契约和日志定向测试：45 项通过。
- 完整回归：630 项通过。
- `compileall`、`pip check` 和 `git diff --check` 通过。
- 本机浏览器验证了统计页直达、7/30 天与指标切换、通用页导航和返回对话；未调用真实模型。
- 本机 Node 因仍链接已移除的 ICU 74 无法执行 `node --check`；实际浏览器成功加载并执行同一 JavaScript，控制流程和页面交互正常。
