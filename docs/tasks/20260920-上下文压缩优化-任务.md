# 上下文压缩优化实施任务

状态：本地实现与回归验证完成。依据[需求](../spec/20260920-上下文压缩优化-需求.md)与[设计](../plan/20260920-上下文压缩优化-设计.md)，2026-09-21。

- [x] 配置目录和页面覆盖 Store：字段及组合校验、版本冲突、原子保存、继承恢复；运行配置单元测试。
- [x] Provider 预算与输出契约：纯预览、实际载荷守卫、输出上限、长度限制终态；运行两家 MockTransport 测试。
- [x] v3 会话迁移：结构化摘要、双边界、旧版本备份与清理；运行 Store 回归。
- [x] Runtime 压缩：有界选批、软保护、降级、冷却、取消与成功提交；运行 Session/Memory 测试。
- [x] 工具逐步预算：定义激活、部分结果下界、停止后续执行及副作用提示；运行 Tool Loop 回归。
- [x] 设置 API 与页面：模型预算和上下文策略、来源、草稿、恢复、保存与冲突；运行 Web 测试并实际验证页面。
- [x] Web/TUI 状态接入：冻结配置、模型切换、阶段和有效上下文占比；运行两入口回归。
- [x] 评测与日志：更新回放协议、指纹和无正文压缩事件，补固定压缩用例。
- [x] 同步中文 README/Knowledge，核对全部需求并执行完整编译、测试、依赖与 Diff 门禁。

不提交或推送，除非用户另行明确要求。

## 2026-09-21 实施记录

- 已完成 `ModelBudget`、环境预算目录、页面覆盖 Store 与有效配置快照服务。
- 两家 Provider 共用真实载荷估算，支持显式输出上限、I/O 前守卫和无副作用工具结果预览；DeepSeek 长度终态、阿里云明确输出限制分别保留。上游输入超限码的严格归一仍待补充证据与测试。
- Runtime 首步/续接与工具结果下界已接入预算检查，回放 Provider 同步协议；测试验证超限后停止后续工具、保留已产生的结果回调。
- Web 设置的四菜单、模型预算表单、上下文策略表单、草稿/恢复/保存、来源显示及严格同源版本化 API 已实现。配置与 Web 相关 50 项测试通过；预算/工具/评测相关 98 项测试通过。
- 临时服务（假模型、临时数据）完成浏览器交互验证：跨页草稿、模型选择锁定、保存与刷新、恢复继承不自动保存。
- 本阶段记录时设置快照尚未接入生产 Web/TUI，压缩仍为原有策略；这些工作已在下文“完成记录”继续实施。
- JavaScript 语法检查使用 Codex 自带 Node；系统 Homebrew Node 缺少 ICU 动态库，未修改共享环境。
- 本阶段完整回归：677 项测试通过；编译、依赖完整性和 Diff 空白检查通过。这只证明当前阶段未回归，不代表未完成项已验收。

## 2026-09-21 完成记录

- 模型页配置当前供应商/模型的窗口与输出预算，上下文页配置全项目压缩阈值、保留轮数与超时/冷却；版本化设置由 Web 保存，Web/TUI 下一次发送读取，浏览器刷新后丢弃未保存草稿。浏览器临时假模型服务已验证菜单、草稿、继承恢复和刷新。
- Provider 首步与每次工具续接均在 I/O 前校验真实载荷估算；摘要使用独立输出上限与输入预算，输入超限不会发起无效摘要。v3 保留完整历史与双边界，v1/v2 读取不改盘、首次成功保存前备份、用户清空移除备份。
- Web/TUI 展示压缩阶段和有效窗口估算；Web 失败/取消提示已落盘的工具文件变更，回答达到输出上限时单独提示。静态代码审查发现并修正完成后占比使用旧工具载荷及清理备份顺序的问题。
- 回放协议与指纹采用冻结的模型预算，离线样例覆盖结构化摘要/旧历史边界/硬预算；压缩新增日志只记录 ID、数字、结果与有限分类，既有模型请求日志仍可能保存正文。
- 不调用真实付费模型。最后完整门禁结果以本次会话最终运行输出为准；阿里云私有域名的真实输出上限支持与真实摘要语义质量需另行获得授权后验证，上游未提供可验证结构化输入超限错误样本时保持原有上游错误类别，不凭状态码猜测。
