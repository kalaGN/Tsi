# 模型响应性能优化任务

状态：已完成（2026-09-20）

- [x] 任务 1：为流式请求增加连接复用和幂等关闭。
  - 验收：同一 Turn 多步骤只创建一个客户端，所有终止路径关闭。
  - 文件：`app/services/llm/`、`app/runtime/chat.py`、Provider 测试。

- [x] 任务 2：记录流式响应阶段耗时。
  - 验收：响应头、首事件、首文本和完整流字段可控、可读且不泄密。
  - 文件：`app/services/llm/http_client.py`、`app/observability/model_logging.py`、日志测试。

- [x] 任务 3：增加有界批量 Workspace 读取。
  - 验收：一次读取 1–4 个片段，保持路径、编码、大小和工具组边界。
  - 文件：`tools/workspace.py`、工具与入口测试、README。

- [x] 任务 4：执行完整门禁并记录结果。
  - 验收：定向测试、完整测试、编译、依赖和 Diff 检查通过。

## 验证记录

- 定向回归：225 项通过。
- 完整回归：619 项通过。
- `compileall`、`pip check` 和 `git diff --check` 通过。
