# Tauri 独立打包任务

对应设计：[Tauri 独立打包设计](../plan/20260923-Tauri独立打包-设计.md)

- [x] 集中处理桌面数据与日志目录。验收：普通启动路径不变，桌面环境覆盖可验证。
- [x] 添加桌面 API 令牌和页面请求 Header。验收：无令牌拒绝、正确令牌通过，普通 Web 不回归。
- [x] 添加 Python sidecar 入口与打包脚本。验收：随机 loopback 端口、就绪输出、静态资源入包。
- [x] 添加 Tauri 2 Rust Host 与最小配置。验收：窗口自动连接 sidecar，退出清理，Rust 编译通过。
- [x] 更新 README 并进行项目检查。验收：安装、构建、首次配置和产物位置可照文档执行。

验证记录：macOS arm64 已生成 `Tsi.app`；桌面窗口实际加载内置 Web UI，关闭后宿主及后端进程均退出；独立后端 `/ui` 返回 200，未授权 `/ui/api/*` 返回 401；完整 Python 测试套件 741 项通过。由于隔离测试目录未配置真实模型密钥，本次未发起真实模型流式请求。
