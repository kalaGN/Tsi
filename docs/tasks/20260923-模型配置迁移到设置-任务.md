# 模型配置迁移到设置：任务

对应设计：[模型配置迁移到设置设计](../plan/20260923-模型配置迁移到设置-设计.md)。

- [x] 1. 加入私有模型配置存储。验收：初始空配置、保存/重读、权限、冲突、损坏文件和秘密脱敏测试。验证：`tests/test_model_config_store.py`。
- [x] 2. 接通生产 Web 与 TUI 模型解析。验收：不依赖 `.env` 模型字段；Web 保存后下一轮请求与 TUI 重启使用文件配置。验证：`tests/test_model_config_production.py`。
- [x] 3. 增加受限 Web 模型配置 API。验收：正文有界、同源、Key 操作显式、GET 不含秘密、更新中请求拒绝。验证：Web API 回归测试。
- [x] 4. 完成设置页模型表单。验收：两 Provider、候选模型、密码输入、保留/替换/删除 Key；保存后界面与下一轮模型调用同步。验证：页面资源测试与 Node 语法检查。
- [x] 5. 更新 README 与迁移说明并整体验证。验收：文档不再要求模型 `.env`，源码/Web/TUI/桌面路径清楚；完整 Python 测试、打包与桌面后端启动通过。

验证结果：`745 passed`；`compileall`、`pip check`、`git diff --check` 与两个 Web JS 文件语法检查均通过；`bash scripts/build_macos_tauri.sh` 成功生成 `Tsi.app`，打包的后端在隔离临时目录输出 `TSI_READY`。桌面 WebView 尚未做人工点击验收。
