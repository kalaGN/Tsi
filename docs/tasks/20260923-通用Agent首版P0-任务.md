# 通用 Agent 首版 P0：实施任务

对应设计：[通用 Agent 首版 P0 设计](../plan/20260923-通用Agent首版P0-设计.md)。

- [x] 1. 抽取固定工具能力目录，不改既有 Web/TUI 可见范围与审批行为。工具组与审批回归测试通过。
- [x] 2. 实现私有任务记录与状态机，覆盖校验、原子保存、冲突和启动中断恢复。
- [x] 3. 实现文件/哈希/固定项目检查的确定性验收器，输出 pass、fail、unknown。
- [x] 4. 接入单轮任务执行器，最多 3 次显式执行；Web 每轮最多 10 分钟，不自动重放不确定写入。
- [x] 5. 接入 Web 创建、列表、查看、继续、取消接口与最小状态 UI；覆盖正常、非法条件及持久状态测试。
- [x] 6. 接入 TUI `/task` 创建、查看与显式继续命令，复用当前请求审批与任务记录格式。
- [x] 7. 补充确定性任务评测场景测试和中文使用文档；完整质量检查通过，未提交。

验证：761 项测试通过；`compileall`、`pip check`、`node --check`、`git diff --check` 通过。测试覆盖超时、中断、持久恢复和显式继续；本轮未进行真实模型请求与桌面打包验证。
